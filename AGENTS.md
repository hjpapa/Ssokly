# Ssokly 개발 작업 지침

## 작업 시작

- 루트의 `README.md`, `STATUS.md`, `VALIDATION.md`를 읽고 현재 동작·미완료 항목·검증 한계를 확인한다.
- 기존 미커밋 변경을 사용자 작업으로 취급하고 보존한다. 문서의 완료 표시는 커밋·배포 완료와 구분한다.

## 작업 기록 갱신

- 코드·기능·설정·문서를 변경한 작업을 마칠 때 같은 작업 안에서 `STATUS.md`를 갱신한다. 별도의 사용자 요청을 기다리지 않는다.
- 상단의 현재 상태·다음 작업을 최신으로 맞추고, 날짜와 같은 날 순번을 붙인 작업 기록을 추가한다.
- 각 기록에는 요청/문제, 변경 사항, 관련 파일, 검증(명령·결과 또는 미실행 사유), 남은 과제·다음 단계를 남긴다. 실패·중단·진행 중은 완료와 구분한다.
- 기존 이력은 삭제하거나 새 작업 결과로 덮어쓰지 않는다. 과거 오류를 발견하면 정정 사유를 기록한다. 과거 대화에서 복원한 기록은 소급 기록임을 표시한다.
- 사용자 동작·설치·설정·데이터 저장 정책이 바뀌면 `README.md`도 함께 갱신한다. README는 현재 사용법만 설명한다.
- 시험을 수행하면 `VALIDATION.md`의 최신 결과 및 관련 기록을 갱신한다. 이전 결과를 재실행한 것처럼 쓰지 않는다. 테스트 수와 날짜·대상·명령·한계를 구분한다.
- 문서만 바꿨다면 링크·현재 코드와의 일치·공백 검사를 하고, 앱 테스트/API를 재실행하지 않았음을 기록한다. 불필요한 유료 API 검증은 하지 않는다.
- 최종 응답에 주요 변경, 실제 검증, 남은 과제와 문서 갱신 여부를 간략히 알린다.

## 기록 보안

- 키·토큰·`.env` 내용·학생/학부모 개인정보·실물 공문 전문·업무 DB 내용을 기록하지 않는다.
- 실물 검증은 필요한 최소한의 비식별 설명과 집계 결과만 남긴다. 개인 PC의 절대 경로는 공유 문서에 넣지 않는다.
- 요청 없는 커밋·푸시·배포·자동화 일정 생성은 하지 않는다. 이 지침은 작업 시 갱신 규칙이며 백그라운드 자동 기록 프로그램은 아니다.
