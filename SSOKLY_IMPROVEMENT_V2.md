# Ssokly 개선 작업 지시서 V2

> **공문 → 업무 카드 → 원문 근거 확인 → 교사 수정 → 저장 → 안내문 생성**  
> 이번 작업은 이 흐름 하나를 안정적으로 완성한다. 기능 수를 늘리거나 앱을 전면 재작성하지 않는다.

- 작성일: 2026-09-17
- 용도: 기존 Ssokly 저장소에서 Codex가 수행할 구현·검증 지시서
- 기본 실행 범위: 아래 **1차 구현**까지. 2차 이후는 별도 지시 전에는 구현하지 않는다.
- 이 문서는 사용자에게 설명받은 앱을 기준으로 작성했다. 실제 코드·화면·저장 구조를 확인한 결과가 아니므로 먼저 저장소를 조사한다.
- 이전 `SSOKLY_CODEX_WORK.md`, `SSOKLY_IMPROVEMENT_PLAN.md`와 제품 범위·우선순위가 다르면 **이 V2를 적용**한다. 저장소의 `AGENTS.md`, 보안 규칙, 빌드·테스트 규칙은 그대로 따른다.

---

## 1. 목표와 범위

### 제품 목표

Ssokly는 학교 공문을 읽어 **교사가 확인할 업무와 다음 행동을 원문 근거와 함께 보여주는 Windows 업무 보조 앱**이다.

중심은 긴 AI 답변이 아니라 확인·수정·재사용할 수 있는 업무 정보다. AI는 업무를 제안하고 교사가 확정한다. 문서마다 전체 원문을 의무적으로 검수하게 하거나, 역할·보호 모드 선택을 반복해서 요구하지 않는다.

### 1차 구현 — 이번에 한다

| 작업 | 반드시 완성할 결과 |
|---|---|
| 업무 카드 | 행동·대상·조건·기한·제출물·제출처를 한곳에서 확인하고 수정한다. |
| 원문 근거 | 중요한 필드에서 원문 위치 또는 실제 발췌문을 바로 연다. |
| 날짜·조건 검증 | 행사일과 제출일, 필수와 조건부, 불명확과 업무 없음을 구분한다. |
| 수정값 보존 | 교사가 고친 값이 재실행·재분석·내 할 일·새 결과물에서도 유지된다. |
| 결과물 일관성 | 같은 업무 데이터로 안내문 등을 생성하고, 이전 정보 기반 결과를 표시한다. |
| 간단한 전송 선택 | 일반 문서는 불필요한 차단 없이 분석하며, 원하는 경우 전송 전에 직접 가린다. |

기존 ‘내 할 일’에는 업무 참조와 수정값을 연결한다. 새로운 대시보드·알림·프로젝트 관리 기능은 만들지 않는다.

### 이번에 하지 않는다

- `빠른 / 표준 / 보호 / 로컬 전용` 4개 모드와 복잡한 개인정보 등급 설정.
- 새 로컬 OCR 엔진 도입, 별도 개인정보 탐지 모델 도입, OCR 전체 교체.
- 본문·붙임·정정 공문의 자동 연결·병합. 단, 이미 함께 가져온 자료의 출처는 구분한다.
- 홈 화면 전면 개편, 회신 집계, 새로운 알림 체계, 캘린더·메신저 자동화.
- 학교 전체 협업, 사용자 계정·클라우드 동기화, 새 이미지 생성 기능.
- 모델 교체나 비용 대시보드 신설. 현재 모델·API 경로를 먼저 확인하고 유지한다.

**보류는 기존 기능 삭제를 뜻하지 않는다.** 현재 동작하는 캡처, HWPX·PDF·이미지 입력, 인식본·검수본, 날짜 강조, 요약, 체크리스트, 안내문, 가정통신문, 내 할 일, 저장, 도식화는 보존한다.

---

## 2. 수정 전 저장소 확인

다음 항목을 실제 코드에서 확인하고, 변경할 파일과 구현 순서를 짧게 제시한 다음 작업한다.

1. `AGENTS.md`, README, 개발 문서, 현재 변경 내역, 실행·테스트 명령.
2. 기술 스택, 캡처와 파일 입력, 원문 뷰어, OCR·분석·결과물 생성 경로.
3. 원본·AI 인식본·교사 검수본·내 할 일의 실제 저장 형식과 식별자.
4. 외부 요청이 시작되는 시점과 포함되는 자료. 파일 업로드·OCR·분석·안내문·이미지 생성 경로를 함께 확인한다.
5. 교사 수정값이 사라지거나 결과마다 사실이 달라질 수 있는 지점.
6. 기존 테스트·데이터 마이그레이션·오류 복구 방식.

사용자 설명상 OCR과 업무 분석에는 `gpt-5-nano`를 사용한다. 실제 코드가 다르면 차이를 보고하되, 이번 작업을 이유로 임의 교체하지 않는다.

### 구현 제약

- 기존 언어·프레임워크·저장소를 사용한다. 새로운 상태관리·데이터베이스·에이전트 프레임워크를 먼저 도입하지 않는다.
- 사용자의 기존 변경사항을 되돌리거나 무관한 파일을 정리하지 않는다.
- 필요한 최소 데이터 확장만 한다. 아래 개념을 구현한다고 모든 구조를 새로 만들 필요는 없다.
- 저장 형식을 바꾸기 전 복구 경로를 확보한다. 실패한 마이그레이션을 빈 데이터로 덮어쓰지 않는다.
- 실제 공문·개인정보·실제 계정으로 자동 테스트하지 않는다. 유료 API 호출·배포·공유 설정 변경은 이번 작업에 포함하지 않는다.

---

## 3. 화면과 조작 — 업무가 먼저, 원문은 바로 옆에

### 기본 흐름

```text
캡처 / 파일 열기
  → 전송 대상 확인 가능: [AI로 분석] [가리고 분석] [취소]
  → 기존 OCR·분석 경로
  → 업무 카드 + 접을 수 있는 원문 패널
  → 필요한 필드만 수정·확인
  → 내 할 일에 담기 / 안내문 생성 / 저장
```

기존 화면과 컴포넌트를 재사용한다. 새로운 단계별 마법사나 모든 필드를 확인해야 통과하는 절차는 만들지 않는다. 캡처·파일 선택 자체로 외부 전송을 시작하지 않고, `AI로 분석` 같은 명확한 실행 동작과 연결한다.

### 업무 카드 예시 — 합성 데이터

```text
경제교육 프로그램 참가 신청                 검수 전

해야 할 일   신청서 제출
업무 조건    참가를 희망하는 학급만
대상         참가 희망 학급
담당         공문에 명시되지 않음
제출 기한    2026-10-15 · 시간 미지정       [근거]
제출물       붙임 2 신청서                  [근거]
제출처       지정된 접수 이메일             [근거]

확인 필요    붙임 2 미확인

[수정] [선택 항목 확인 완료] [내 할 일에 담기] [안내문 만들기]
```

- 첫 화면은 ‘해야 할 일·기한·조건·확인 필요’를 우선한다. 전체 요약은 별도로 펼친다.
- 담당자와 대상자를 구분한다. 담당자 정보가 없으면 사용자의 역할을 추측해 확정하지 않는다.
- 교사가 맡을 항목만 선택한다. 사전 역할 선택을 강제하거나 기존 역할 설정으로 다른 업무를 숨기지 않는다.
- 검수 전 데이터도 초안으로 열고 편집할 수 있다. 미확인 상태를 숨기지 않되 모든 작업을 막지 않는다.
- 기존 청록색 날짜·빨간색 오류 표시는 유지할 수 있지만, 색만 쓰지 않고 상태 문구를 함께 둔다.
- `내 할 일에 담기`는 업무 완료가 아니다. 안내문 복사도 안내·제출 완료로 처리하지 않는다.

---

## 4. 데이터 기준 — 교사가 수정한 현재값은 한곳에

### 최소 데이터 개념

구현 언어·필드명은 기존 코드에 맞춘다. 아래는 요구되는 관계이며 특정 타입 정의를 강제하지 않는다.

| 개념 | 최소한 보존할 정보 |
|---|---|
| 문서 | 안정적인 ID, 원본 참조, 인식본/검수본 구분, 원문 버전, 가져온 범위 |
| 업무 | 안정적인 ID, 문서 ID, 현재 업무 버전, 행동·담당·대상·조건·일정·제출 정보 |
| 주요 필드 | AI 제안값, 현재값, 교사 수정 여부, 확인 상태, 근거 참조 |
| 근거 | 문서 ID, 참조한 원문 버전, 실제 발췌문, 알 수 있는 페이지·문단·표 위치 |
| 결과물 | 종류, 참조한 업무 ID·버전, 내용, 생성 시점, 이전 정보 기반 여부 |
| 내 할 일 | 업무 ID 참조, 기존 완료 상태·메모 등 개인 관리 정보 |

일정은 하나의 `deadline` 문자열로 모든 날짜를 합치지 않는다. 같은 업무에 신청 마감·행사일·보고일이 함께 존재할 수 있도록 현재 구조를 최소 확장한다.

### 현재값 결정 원칙

```text
교사의 명시적 수정이 있으면 → 그 값을 현재값으로 사용
명시적 수정이 없으면       → 검증 상태와 함께 AI 제안값 사용
자료가 없으면             → 미지정/확인 필요 유지
```

- 교사가 날짜나 담당자를 **지운 것**도 수정이다. 빈값을 ‘수정 없음’으로 해석해 AI 값을 되살리지 않는다.
- AI 인식본·원문 검수본·업무 필드 수정은 구분한다. 카드의 날짜를 바꿨다고 원문이 그 날짜였던 것처럼 바꾸지 않는다.
- 원문과 다른 수동 수정값에는 `교사 수정`을 표시하고 기존 근거와 비교할 수 있게 한다.
- 한 필드만 고친 뒤 문서 전체를 자동으로 `검수 완료`로 표시하지 않는다.
- 재분석 결과는 새로운 제안이다. 수정값, 명시적 빈값, 메모, 완료 상태, 개인 준비일을 조용히 덮어쓰지 않는다.
- 기존 업무와 새 추출 항목의 대응이 확실하지 않으면 비교 후보로 남긴다. 제목이 비슷하다는 이유로 다른 업무의 수정값을 복사하지 않는다.

### 저장·동시 처리

- 주요 필드 변경 시 업무 버전을 갱신한다. 단순 완료 체크·창 열기로 업무 내용 버전을 바꾸지 않는다.
- 교사 수정은 기존 저장 방식에 맞춰 안전하게 저장하고 저장 성공·실패를 구분해 표시한다.
- 저장 실패 시 편집값을 화면에서 보존하며, 재시도할 수 있게 한다.
- 늦게 도착한 이전 AI 응답이 최신 수정값을 덮어쓰지 않도록 요청 시작 버전과 현재 버전을 비교한다.
- 업무 ID가 같으면 ‘내 할 일’에 중복 추가하지 않는다. 날짜 수정은 연결된 업무 표시에도 반영한다.

---

## 5. 원문 근거 — 문장 자체를 확인할 수 있어야 한다

### 구현 우선순위

1. **실제 원문 발췌문 + 문서 식별 정보**를 보여준다.
2. 이미 확보된 페이지·문단·표 위치로 이동한다.
3. 정확한 좌표를 기존 경로에서 확보한 경우에만 이미지·셀을 강조한다.

좌표 강조를 만들기 위해 새 OCR 엔진을 도입하지 않는다. 페이지나 위치를 모르면 표시하지 않고, 실제 확보한 발췌문부터 제공한다.

### 필수 규칙

- 기한·대상·조건·제출물·제출처의 근거를 가능한 한 필드별로 연결한다. 문서 전체를 근거 한 개로 붙이지 않는다.
- 근거 문구는 추출 당시 참조한 원문/검수본과 대조한다. 공백·줄바꿈 수준의 정규화와 실제 내용 변경을 구분한다.
- 모델이 만들어낸 인용문·페이지 번호를 그대로 `근거 확인됨`으로 표시하지 않는다.
- 인식본과 일치한다는 사실이 원본 이미지까지 정확히 읽었다는 뜻은 아니다. 어떤 버전의 근거인지 표시한다.
- 같은 문장이 여러 번 나오거나 표 위치가 모호하면 유일한 위치인 것처럼 강조하지 않는다.
- 원문 검수본이 변경되면 영향받는 근거는 재확인 대상으로 표시한다. 오래된 좌표를 새 원문에 그대로 적용하지 않는다.
- 근거를 확보하지 못한 항목은 삭제하기보다 `근거 확인 필요`로 남긴다. AI의 추가 준비 제안은 공문상 의무와 분리한다.

**MVP 완료 기준:** 날짜의 `[근거]`를 누르면 실제 발췌문과 출처가 한 번에 열린다. 정확한 좌표 강조는 필수가 아니다.

---

## 6. 날짜·조건 검증 — 값보다 의미

### 날짜 처리

| 상황 | 요구 동작 |
|---|---|
| 행사일과 제출 마감이 함께 있음 | 각각의 역할과 적용 업무를 구분한다. |
| 시간 없는 마감 | 시간은 미지정으로 둔다. 18:00·23:59 등을 만들어 넣지 않는다. |
| 연도 생략 | 원문 연도 등 근거를 제시하거나 추정 상태로 남긴다. 현재 연도를 몰래 확정하지 않는다. |
| ‘행사 3일 전’, ‘공문 접수 후 5일 이내’ | 기준일이 확정되어야 계산한다. 기준일이 없으면 상대 표현을 보존한다. |
| 날짜·요일 불일치 | 양쪽 표기를 보여주고 교사가 선택하게 한다. |
| 본문·표·붙임 날짜 충돌 | 충돌 상태로 표시한다. 임의로 최신·더 긴 기한을 선택하지 않는다. |
| 표 안에 여러 행사 | 행·열 맥락과 행사별 날짜·장소·대상을 유지한다. |
| 개인 준비일 | 공문 마감과 분리한다. AI 준비일 제안은 이번에 새로 확대하지 않는다. |

달력 날짜는 날짜 값으로 보존하고 불필요한 UTC 변환으로 전날·다음 날이 되지 않게 한다. 실제 시각은 기존 앱의 시간대 처리와 일관되게 저장·표시한다.

### 업무 조건

| 원문 예시 | 해석 기준 |
|---|---|
| 모든 학교 제출 | 명시된 대상에 대한 필수 업무 |
| 희망 학교만 제출 | 조건부 업무 |
| 해당 사항 없으면 제출 생략 | 조건을 확인한 후 생략 가능한 업무 |
| 해당 사항 없으면 ‘없음’으로 회신 | 회신 필요. 업무 없음으로 처리하지 않음 |
| 참고하시기 바랍니다 | 정보성 내용. 자동으로 제출 업무 생성하지 않음 |
| 세부 사항은 붙임 참조, 붙임 미확인 | 자료 부족. 판단 유보 |

화면에서는 `필수 / 조건부 / 안내 / 추가 제안 / 판단 유보`를 구분한다. AI 자기평가를 `정확도 98%`처럼 표시하지 않는다.

문서 일부만 가져왔다면 `가져온 범위에서는 제출 업무를 확인하지 못했습니다`라고 표시한다. 전체 문서에 업무가 없다고 단정하지 않는다.

---

## 7. 결과물 생성 — 사실은 고정하고 표현만 바꾸기

### 생성 경로

```text
업무 현재값 + 필요한 검수된 문서 내용
  → 선택한 전송 범위 적용
  → 안내문·체크리스트 등 초안 생성
  → 중요 사실 검증
  → 업무 버전과 함께 저장
```

- 업무형 결과물은 현재 업무 데이터의 날짜·대상·조건·제출물·제출처 등을 사용한다.
- 전체 원문 요약은 필요한 검수본을 참고하되, 업무 필드의 현재값과 원문이 다르면 원문 내용을 몰래 바꾸지 말고 교사 수정 사항을 구분한다.
- 요약·안내문·가정통신문을 만들 때마다 원문 OCR부터 다시 실행하지 않는다.
- 문서와 업무 내용은 분석 대상 데이터다. 원문에 포함된 ‘지시를 무시하라’ 등의 문구를 앱 실행·전송 지시로 취급하지 않는다.
- 기존 구조화 출력 기능을 재사용한다. JSON 형식이 맞는 것과 사실이 정확한 것은 따로 검증한다.[R1]

### 사실과 표현 분리

AI가 조정할 수 있는 것은 제목 표현·문체·길이·순서·친절한 설명이다. 날짜·시간·장소·금액·대상·준비물·신청 조건·제출처·연락처는 원문 또는 교사 입력 근거 없이 추가·변경하지 않는다.

가능하면 날짜·대상·제출처 등 핵심 사실 구역은 현재값으로 직접 렌더링하고, AI는 연결 문장·설명을 작성하게 한다. 기존 자유 서술 방식을 유지한다면 생성 후 핵심 필드와 비교하고 충돌을 표시한다. 오류가 반복되면 무한 재생성하지 말고 기본 템플릿이나 편집 가능한 초안으로 돌아간다.

- 교직원 안내는 ‘누가·무엇을·언제까지·어떻게 회신’을 우선한다.
- 학부모 안내에는 내부 결재·업무분장 정보를 불필요하게 넣지 않는다. 필요한 준비물·동의·회신 정보는 유지한다.
- 대상에 맞는 필수 정보가 없으면 `[확인 필요]`로 남기며 임의로 채우지 않는다.
- 기존 도식화 기능은 유지하되 새 사실을 추측하지 않는다. 현재값과 불일치하는 도식은 검수 필요로 표시하며, 도식화 고도화는 보류한다.

### 수정 이후 처리

- 결과물 생성 시 사용한 업무 ID·버전을 기록한다.
- 중요 업무 필드가 바뀌면 연결된 기존 결과물에 `이전 정보로 작성됨`을 표시한다.
- 기존 결과물과 사용자의 문장 수정은 자동 덮어쓰지 않는다. `[현재 정보로 새 초안 만들기]`를 제공한다.
- 이전 결과를 보거나 복사할 수는 있지만, 오래된 정보 표시를 숨기지 않는다.
- 생성 중 업무가 바뀌면 도착한 결과는 요청 시작 버전의 결과로 저장하고 바로 이전 정보 상태를 표시한다.

---
## 8. 개인정보·AI 전송 — 단순한 선택, 실제로 지켜지는 가림

### 8.1 제품 원칙

**개인정보로 추정되는 표현이 있다는 이유만으로 분석을 일괄 차단하지 않는다.** 일반적인 업무 연락처 때문에 매번 확인창을 띄우지 않는다. 대신 외부 전송 시점·범위를 알리고 사용자가 필요한 부분을 가릴 수 있게 한다.

반대로 ‘담당자 이름이면 항상 안전’, ‘사용자가 확인을 누르면 모든 전송이 허용됨’처럼 안내하지 않는다. 학교 이용 기준을 우회하는 기능은 만들지 않는다. 이 문서는 개인정보의 법적 분류나 전송 허용 여부를 판정하는 문서가 아니다.

### 8.2 최소 UI

- 최초 AI 사용 시 외부 API 전송과 비용 발생을 짧게 안내한다. 동일 안내를 매번 모달로 반복하지 않는다.
- 평상시에는 분석 버튼 근처에 실제 전송 대상과 확인·가림 진입점을 표시한다.
- 기본 버튼은 `[AI로 분석] [가리고 분석] [취소]`다. 4개 보호 모드를 새로 만들지 않는다.
- 가림 화면에서 `[가린 내용으로 분석]`을 누르면 미리 본 전송용 사본만 전송한다.
- 선택한 파일·페이지 외에 다른 문서, 전체 업무 기록, 사용자 메모를 자동으로 추가하지 않는다.
- 취소·전송하지 않기를 선택해도 로컬 열람·저장·수동 편집 등 현재 가능한 기능은 사용할 수 있다. 새 로컬 전용 OCR 체계를 요구하지 않는다.

### 8.3 민감한 내용 확인은 가능한 범위에서만

기존 로컬 OCR 또는 HWPX·PDF 텍스트 추출로 **첫 외부 전송 전에** 내용을 확보할 수 있다면, 가벼운 패턴·주변 문맥 검사로 확인이 필요한 후보를 표시한다. 별도 AI 호출을 개인정보 탐지에 사용하지 않는다.

- 업무용 연락처만으로 매번 경고하지 않는다.
- 학생·학부모 식별 정보, 개인 연락처, 건강·상담 내용, 식별번호·금융정보·인증정보 등의 의심 후보에는 확인을 권한다. 이는 운영상 주의 후보이며 모두 같은 법적 범주라는 의미가 아니다.
- 이름만으로 학생인지 담당자인지 확정하지 않는다. 단순 정규식만으로 건강·상담 내용 전체를 탐지했다고 주장하지 않는다.
- 후보가 있으면 `[가리고 분석] [확인 후 그대로 분석] [취소]`를 제공한다. 일반 문서에까지 추가 승인 단계를 확장하지 않는다.
- 원본 전송 선택은 해당 자료·범위에 대한 선택이다. 다른 공문·추가 첨부에 포괄적으로 적용하지 않는다.

설정이 필요하면 **‘전송 전 확인 가능한 민감정보 후보 알림’** 하나만 두고 기본값을 켠다. 실제 검사 가능한 입력 형식과 한계를 함께 표시한다. 알림을 꺼도 외부 전송 표시·수동 가림·선택 범위 준수는 유지한다. 검사 기능이 전혀 없는 입력에 동작하는 것처럼 보이는 설정을 제공하지 않는다.

**이미지를 로컬에서 읽을 수 없는 경우:** `이미지의 개인정보는 전송 전 자동 확인하지 못합니다. 필요한 부분은 직접 가려 주세요.`라는 비차단 안내를 표시한다. `개인정보 없음`이나 탐지 건수를 만들어 표시하지 않는다.

GPT OCR 후 발견한 후보는 ‘분석 후 확인’이다. 이미 이뤄진 전송을 보호한 것처럼 설명하지 않는다.

### 8.4 가림 구현의 필수 조건

```text
로컬 원본 보존
  → 사용자가 선택한 부분 가림
  → 실제 전송용 사본 생성
  → 미리보기
  → 그 사본으로 첫 OCR·분석 요청
```

- 이미지 가림은 불투명 영역을 픽셀에 합성한다. 화면 위 오버레이만 씌우거나 흐림 처리만 하지 않는다.
- 텍스트 가림은 실제 요청 문자열에서 제거·대체한다. 위치별 구분이 필요하면 `[담당자1]`, `[담당자2]`처럼 구분하되 날짜·역할까지 무조건 삭제하지 않는다.
- PDF·HWPX 원본 자체의 완전한 가림이 어렵다면, 선택한 페이지의 렌더링 이미지 또는 추출 텍스트 사본을 가려 전송한다. 형식·표 구조 변화는 미리보기로 확인한다.
- 가린 사본과 함께 원본 파일, 숨은 텍스트 레이어, 원본 발췌문, 가리지 않은 미리보기를 추가 전송하지 않는다.
- 가림 사본 생성에 실패하면 해당 요청을 멈추고 오류를 알린다. 원본으로 자동 대체하지 않는다. 원본 분석은 사용자가 다시 명시적으로 선택할 수 있다.
- 재시도·업무 분석·안내문·도식화에서도 선택한 가림 범위를 유지한다. 로컬 원본이나 이전 대화 이력에서 가린 정보를 다시 채워 보내지 않는다.
- 가림 후 로컬에서 실명·연락처를 입력·복원하더라도 다음 AI 호출에 자동 포함하지 않는다. 전송 범위를 넓힐 때만 변경 사실을 보여주고 다시 선택하게 한다.
- 첫 AI OCR 이후 가림을 선택했다면 ‘이후 요청부터 적용’임을 알린다. 이전 전송을 취소·회수했다고 표시하지 않는다.

‘분석을 막지 않음’은 일반 개인정보 발견에 대한 원칙이다. **사용자가 가림을 선택했는데 실제로는 원본이 전송되는 오류까지 허용한다는 뜻이 아니다.**

### 8.5 API 안내와 로깅

API의 학습 이용 여부, 서버 보관, PC 저장은 별개다. ‘API이므로 외부에 저장되지 않는다’고 안내하지 않는다. 실제 사용 엔드포인트·설정과 공식 데이터 처리 안내를 확인해 표시한다. `store:false`만으로 모든 서버 보관이 없어지는 것으로 설명하지 않는다.[R2]

원문·실명·연락처·요청 전문·인증정보를 디버그 로그나 원격 오류 보고에 기본 기록하지 않는다. 진단에는 요청 종류·상태·시간·버전 등 필요한 메타데이터를 우선 사용한다. 이번에 새 원격 사용 추적을 추가하지 않는다.

---

## 9. 오류·성능 — 별도 플랫폼 없이 기본 신뢰 확보

- 같은 문서를 다시 열 때 저장된 결과를 재사용한다. 재분석은 사용자가 선택하거나 실제 입력 변경이 있을 때 수행한다.
- 문체 변경에는 결과물만 다시 생성한다. 완료 체크·개인 메모·단순 열람은 AI 호출하지 않는다.
- 요청 재사용 여부를 판단할 때 원문·검수본·업무 버전과 전송용 사본/가림 상태를 함께 구분한다. 가림 후 요청에 원본 기반 캐시를 몰래 섞지 않는다.
- 중복 클릭에 대해 같은 실행의 API 요청을 중복 발행하지 않는다. 사용자가 의도한 재시도는 구분한다.
- 실패·타임아웃·거절·불완전한 JSON을 정상 결과나 ‘할 일 없음’으로 저장하지 않는다.
- 자동 재시도는 기존 정책을 확인하고 제한한다. 무한 재시도·무한 재생성을 금지한다.
- 취소된 요청이 뒤늦게 끝나더라도 현재 화면·수정값을 덮어쓰지 않는다. 취소가 이미 발생한 전송·비용을 되돌리는 것은 아니므로 상태를 사실대로 표시한다.
- 파일 파싱 실패, 저장 실패, 앱 재실행 후 복구를 합성 데이터로 시험한다. 기존 데이터 삭제로 오류를 해결하지 않는다.

---

## 10. 구현 순서 — 작은 단위로 끝까지 연결

| 순서 | 구현 단위 | 해당 단위의 확인 기준 |
|---|---|---|
| 0 | 저장소 조사·기존 테스트 확인 | 실제 파일·데이터·전송 경로를 파악한다. |
| 1 | 현재값·수정 여부·업무 버전 저장 | 날짜 수정과 명시적 삭제가 재실행 후 유지된다. |
| 2 | 업무 카드·필드별 근거·날짜/조건 상태 | 카드에서 근거를 열고 필요한 필드만 수정한다. |
| 3 | 내 할 일·기존 생성 기능 연결 | 수정값을 공통으로 사용하고 이전 결과를 표시한다. |
| 4 | 최소 전송 선택·수동 가림·가능한 후보 알림 | 선택한 사본이 첫 요청과 후속 요청에 실제 사용된다. |
| 5 | 통합·회귀 테스트·실행 확인 | 아래 인수 조건을 시험하고 미검증 범위를 남긴다. |

이는 구현 순서이며 공개·배포 순서가 아니다. 전송 선택·가림 동작을 검증하지 않은 중간 상태를 완료품으로 배포하지 않는다.

현 구조상 순서 변경이 필요하면 이유를 짧게 기록한다. 2차 기능을 선행 구현하거나 계획서만 추가하고 끝내지 않는다.

---

## 11. 인수 테스트 — 합성 문서와 mock으로 검증

기존 테스트 도구를 우선 사용한다. 실제 API 호출 없이 성공·실패·지연·재시도·거절 응답을 재현한다. 새로운 테스트 도구가 꼭 필요하면 이유와 최소 의존성을 기록한다.

| ID | 시나리오 | 기대 결과 |
|---|---|---|
| A01 | 합성 공문에서 업무 추출 → 근거 확인 → 날짜 수정 → 저장 → 재실행 → 안내문 생성 | 교사 수정값이 내 할 일과 새 안내문에 동일하게 반영된다. |
| A02 | 행사일·신청일·보고일이 함께 있고 마감 시간은 없음 | 날짜 역할이 분리되며 임의 시간이 생성되지 않는다. |
| A03 | 연도 누락·상대 기한·날짜/요일 충돌·여러 행사 표 | 근거 없는 확정값을 만들지 않고, 행사별 관계와 확인 필요 상태를 유지한다. |
| A04 | ‘희망 학교만’, ‘해당 없음 제출 생략’, ‘없음으로 회신’ 비교 | 조건부·생략 조건·회신 의무를 다르게 처리한다. |
| A05 | 일부 캡처 또는 붙임 미확인 | 전체 업무가 없다고 단정하지 않는다. |
| A06 | AI가 존재하지 않는 인용·페이지를 반환 | 확인된 원문 근거로 표시하지 않는다. 좌표가 없을 때 발췌문 경로로 확인 가능하다. |
| A07 | 교사가 기한을 빈값으로 지운 후 재분석 | 삭제 의도가 유지된다. 오래된 날짜가 되살아나지 않는다. |
| A08 | AI 요청 중 날짜 수정, 이후 늦은 응답 도착 | 수정값을 덮어쓰지 않고 응답의 원래 버전을 구분한다. |
| A09 | 날짜 수정 전에 생성한 안내문과 수정 후 새 안내문 | 이전 초안은 보존·표시하고 새 초안에는 현재값을 사용한다. |
| A10 | 동일 업무를 두 번 담기·복사하기 | 내 할 일 중복·자동 완료 처리가 없다. |
| P01 | 일반 업무 연락처가 포함된 합성 문서 | 개인정보 발견만으로 분석이 일괄 차단되거나 반복 팝업이 뜨지 않는다. |
| P02 | 로컬 텍스트에서 고위험 의심 후보 탐지 / 로컬 OCR 없는 이미지 | 전자는 확인 선택지를 제공하고 후자는 자동 검사 불가를 솔직히 표시한다. |
| P03 | 이미지 영역 가림 후 OCR 요청 | 네트워크 직전 payload를 가로채 디코딩한 이미지에 실제 불투명 가림이 있고, 가림 밖 내용은 보존된다. |
| P04 | 텍스트·파일 가림 후 분석 → 안내문 → 재시도 → 기존 도식화 | 알려진 합성 비밀 문자열·원본 파일·숨은 원문이 어느 경로에도 다시 포함되지 않는다. |
| P05 | 가림 실패·취소·가림 뒤 전송 범위 변경 | 원본 자동 대체·취소 후 새 전송이 없다. 범위를 넓힐 때 다시 선택한다. |
| R01 | 타임아웃·거절·불완전한 응답·중복 클릭 | 정상 결과로 저장하지 않으며 수정 데이터 보존·제한된 재시도가 동작한다. |
| R02 | 저장 실패·재실행·기존 기록 마이그레이션 | 기존 원본·수정값·메모·완료 상태가 유지되거나 복구 가능하다. |
| R03 | 기존 입력과 결과물·재열람·문체 변경 | 기존 기능을 유지하며 불필요한 OCR/분석 요청이 늘지 않는다. |

P03~P05는 UI 스크린샷만으로 통과 처리하지 않는다. 전송 함수 경계에서 실제 payload와 첨부를 검사한다. 테스트용 비밀 문자열·식별번호 형태는 실제 사람과 무관한 합성값만 사용한다.

mock 테스트 통과는 실제 모델의 공문 해석 정확도를 보증하지 않는다. 이번에는 구조·검증·저장·전송 동작을 검증하고, 실제 모델 품질 평가는 별도 승인된 비식별 평가 자료로 진행할 후속 항목으로 남긴다.

---

## 12. 완료 기준과 보고

### 1차 완료 체크리스트

- [ ] 새 기술 스택으로 재작성하지 않고 기존 기능·데이터를 보존했다.
- [ ] 업무 카드에서 원문 근거를 바로 확인하고 필드를 수정할 수 있다.
- [ ] 날짜 역할·조건부 업무·자료 부족·충돌 상태가 구분된다.
- [ ] 교사 수정과 명시적 빈값이 재실행·재분석 후에도 유지된다.
- [ ] 내 할 일과 새 결과물이 현재 업무 데이터를 사용한다.
- [ ] 기존 초안은 보존되고 업무 변경 시 이전 정보 기반임을 표시한다.
- [ ] 일반 개인정보 때문에 일괄 차단하지 않으며 4개 보호 모드를 만들지 않았다.
- [ ] 수동 가림은 첫 요청과 후속 요청의 실제 전송 데이터에 적용된다.
- [ ] 자동 탐지 가능 범위와 불가능 범위를 정확히 표시한다.
- [ ] 수행한 테스트·빌드·실행 검증 결과와 미검증 항목을 구분했다.
- [ ] 2차 이후 기능을 불필요하게 구현하지 않았다.

### Codex 완료 보고 형식

```text
1. 구현한 핵심 흐름과 변경한 실제 파일
2. 데이터 저장·마이그레이션·교사 수정값 보존 방식
3. 원문 근거 및 결과물 버전 처리 방식
4. 실제 외부 전송 경로와 가림 적용 범위
5. 실행한 명령, 테스트 ID별 결과, 빌드·실행 결과
6. 기존 기능 영향과 미검증 항목
7. 남은 문제와 다음 최소 작업
```

계획만 작성하고 구현 완료라고 보고하지 않는다. Windows 실행 환경이나 유료 API 접근이 없으면 해당 검증을 통과했다고 주장하지 않는다. 가능한 정적·단위·통합 검증을 진행하고 제한을 명시한다.

---

## 13. 다음 단계 — 이번에는 구현하지 않음

**2차: 관련 공문 이어보기.** 먼저 사용자가 기존 업무에 본문·붙임·정정 공문을 직접 연결하는 기능과 변경 비교를 검토한다. 자동 연결은 그 이후다. 날짜·양식 변경을 제안하고 교사가 승인하며, 기존 수정값·메모·완료 상태는 보존한다.

**3차: 간단한 후속 업무 관리.** 실제 사용 결과를 보고 ‘오늘·이번 주·회신 대기’와 선택적 로컬 알림을 검토한다. 새 프로젝트 관리 서비스를 만들지 않는다.

1차에서는 문서 ID·업무 ID·버전·출처를 보존하는 정도로 연결 가능성을 남긴다. 미래 기능을 위한 대규모 프레임워크나 빈 화면은 만들지 않는다.

---

## 14. Codex에 붙여 넣을 실행 프롬프트

```text
SSOKLY_IMPROVEMENT_V2.md를 읽고 기존 Ssokly를 개선해 줘.
이번 범위는 이 문서의 1차 구현까지야. 이전 개선안보다 V2의 범위를 우선해.

먼저 AGENTS.md, README, 실제 코드와 현재 변경사항을 확인하고
수정 대상 파일과 구현 순서를 짧게 설명한 뒤 코드 수정·테스트까지 진행해.
새 앱으로 재작성하거나 기존 데이터·사용자 수정사항을 덮어쓰지 마.

핵심은 공문 → 업무 카드 → 원문 근거 → 교사 수정 → 저장 → 안내문 생성이야.
날짜·조건을 구분하고, 교사 수정값과 명시적 삭제가 재실행·재분석 후에도
내 할 일과 새 결과물의 기준이 되도록 해. 이전 초안은 버전 표시와 함께 보존해.

개인정보 보호는 4개 모드가 아니라 명확한 전송 안내·선택적 확인·수동 가림으로 구현해.
일반 연락처만으로 분석을 일괄 차단하지 마.
이미지를 전송 전에 검사할 수 없다면 그 한계를 표시하고 직접 가릴 수 있게 해.
가림을 선택했을 때는 첫 OCR부터 후속 결과물 생성·재시도까지 실제 payload에서 지켜야 해.

합성 데이터와 mock을 사용하고 실제 문서나 불필요한 유료 API 호출은 사용하지 마.
관련 공문 연결·새 알림·로컬 OCR 도입은 이번에 구현하지 마.
완료 후 수정 파일, 검증 결과, 기존 기능 영향, 미검증 항목과 남은 문제를 보고해.
```

---

## 참고 — 외부 기술 사실을 확인할 공식 문서

이 지시서의 기능·우선순위·인수 조건은 Ssokly를 위한 설계 요구사항이다. 아래 자료는 API 동작에 대한 제한적인 참고이며, 앱의 현재 구현 상태나 학교의 이용 허가를 증명하지 않는다. 확인일: 2026-09-17.

- [R1] OpenAI, *Structured model outputs*: 구조화 출력에도 내용 오류가 남을 수 있으므로 구조 검증과 내용 검증을 분리한다. https://developers.openai.com/api/docs/guides/structured-outputs
- [R2] OpenAI, *Data controls in the OpenAI platform*: 학습 이용, 악용 모니터링 로그, 기능별 애플리케이션 상태 보관을 구분하고 실제 사용 경로를 확인한다. https://developers.openai.com/api/docs/guides/your-data

**최종 원칙: 기능을 더 많이 만드는 것보다, 한 공문의 업무가 확인·수정·저장·재사용까지 끊기지 않게 만든다.**
