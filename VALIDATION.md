# Ssokly 사용 검증 기록

마지막 기능 검증일: 2026-09-17. 문서 정비일: 2026-09-17.

[현재 사용법](README.md) · [작업 상태와 이력](STATUS.md)

## 단계별 후속 검증 — 2026-09-17

### 캡처 중심 재구성 1단계: 기반 검증

- 최종 `.venv\Scripts\python.exe -m unittest tests.test_document_library tests.test_desk_widgets -q`: **38개 통과, 2.147초**. 저장 계층 신규 28개와 이미지/인라인 표/썸네일 위젯 신규 10개. 기본 진입점은 아직 변경하지 않았으며 전체 앱 시험 결과로 해석하지 않음.
- 저장 집중 `.venv\Scripts\python.exe -m unittest tests.test_document_library tests.test_capture_inbox_store tests.test_task_store tests.test_personal_todos -q`: **63개 통과, 2.170초**. 임시 DB의 빈 수정값/재OCR/검색/순서/이력/CAS/휴지통/실제 commit 실패 rollback/가산 migration 및 백업/기존 데이터 보존 검사.
- 위젯은 허용된 Windows Tk 환경에서 합성 이미지로 검사. 기본 sandbox의 init.tcl 접근 오류는 실행 환경 제약으로 구분하며, 숨긴 Tk의 resize는 바인딩된 handler 호출로 검증. 실제 마우스/배율 전수 확인은 아님. 클립보드는 직접 쓰지 않고 callback 인자로 검사.
- 초기 결과 이력 시간 동률의 정렬 문제를 보완했고 저장 신규 28개에 회귀 포함. 실제 업무 DB·API·공문 미사용, 기존 모델 품질 시험을 다시 실행한 것이 아님.

### 긴 표·병합 셀·표 보기 개선 검증

- 합성 HWPX·전사 텍스트·임시 저장소·Tk 위젯을 사용하며 실제 공문/사용자 DB·API·키를 검증 입력으로 사용하지 않음. 클립보드 호출은 mock으로 검사하여 사용자 클립보드는 덮어쓰지 않음.
- 중간 `.venv\Scripts\python.exe -m unittest tests.test_hwpx_structure tests.test_table_schedule_context tests.test_source_tables_ui tests.test_source_review tests.test_v2_app_flow -q`: **60개 통과, 10.588초**.
- 중간 `.venv\Scripts\python.exe -m unittest discover -s tests -q`: **381개 통과, 33.533초**. 이후 독립 검토의 행사명 반복 머리글·병렬 행사 열·셀/행을 넘나드는 날짜 강조 3종 결함에 회귀 4개를 추가함. 이 수치는 최종 결과가 아님.
- 최종 같은 전체 명령: **385개 통과, 54.678초, 건너뜀 없음**. 직전 단계 346개에 표 일정 15개·HWPX 15개·표 UI/검출 7개·검수 1개·실제 앱 연결 1개 추가.
- 최종 `.venv\Scripts\python.exe tools/verify_v2_release.py`: **228개 통과, 25.268초**, 실패/오류/건너뜀/통신 시도 모두 0. 새 HWPX/표 일정/표 UI 및 기존 원문 검수 모듈을 차단 검증 목록에 추가.
- 추가 회귀의 과정: 행사명 반복·병렬 행사 2개는 수정 전 실패 재현 후 집중 82개 통과(0.441초). 셀/행별 날짜 강조 추가 시험에서는 테스트의 기대 원문 행 번호 오기 1개로 집중 24개 중 1개 실패 → 기대값 정정 후 24개 통과(0.434초). 위 최종 전체 수치는 이 정정 이후 결과.
- 최종 `.venv\Scripts\python.exe tools/verify_release.py`: `passed: true`, `live: false`. `compileall -q services ui tests tools`, `pip check`, README/STATUS/VALIDATION의 로컬 링크, `git diff --check` 통과. 기존 Pillow 폐기 예정 경고와 의도적 재검토 실패 로그는 테스트 실패가 아님.
- HWPX: 122행·앞뒤/중간 빈 셀·가로/세로 병합·빈 병합·중첩 표·줄바꿈/탭과 XML tail·좌표 기반 열 순서·기존 단일열 문단/누락 span 호환. 잘못된 좌표/차원/span·겹침·메타데이터 중복·섹션 이름/번호의 안전 오류를 검사.
- 표 일정: 2·3단 머리글·반복 머리글 순서 변경·다른 행사 셀의 비날짜 값 혼입·앞쪽 분류 열·빈 제목/열 수 불일치/미지원 머리글의 위치 안내·CRLF 100행/200날짜의 정확한 문자/행/셀 위치를 검사.
- UI: 첫 행 상세·빈 셀·원문 행 번호·유효 날짜와 잘못된 날짜의 개별 강조·긴 내용 및 ragged 행의 복사·선택한 표 탭·병합 표시 보존. 실제 앱의 표 창 호출이 원문/변경 버전/업무 카드를 변경하지 않는지 검사. OS 실제 붙여넣기·사용자 마우스 손 조작·실물 지면 일치 확인은 아님.

### 재시작 후 재개 검증

- 재개 기준 `a575f16`: `.venv\Scripts\python.exe tools/verify_v2_release.py` 161개 통과(14.230초), 실패·건너뜀·통신 시도 0. Git fetch 후 원격 main 차이 0/0.
- 중간 종료 재현: 초안 이력 저장 후 업무 본문 저장 전에 앱 객체 종료/재생성. 새 회귀 1개는 수정 전 실패(이전 본문에 새 버전 연결), 수정 후 `.venv\Scripts\python.exe -m unittest tests.test_v2_app_flow -q` **22개 통과, 4.729초**. 일치 이력 복구·알 수 없는 본문 보존·다중 후보 미확인 표시 3개 신규 회귀 포함.
- 모두 합성/mock·임시 저장소. 실제 OS 강제 종료·전원 손실·디스크 손상 시험이나 유료 API는 수행하지 않음.
- 후속 최종 `.venv\Scripts\python.exe -m unittest discover -s tests -q`: **346개 통과, 32.262초, 건너뜀 없음**. 기준 321개에 복구 3개·시간 표현 9개·저장 실패 주입 13개 추가.
- `.venv\Scripts\python.exe tools/verify_v2_release.py`: **186개 통과, 13.560초**, 실패/오류/건너뜀/통신 시도 모두 0. 신규 `tests.test_storage_faults`도 차단 검증에 포함.
- 시간 표현: 반·초·대략 시각·연속 범위의 원문 표기, 명시 AM/PM 앞/뒤 및 24시간 비교, 오전/오후 일부 생략 범위의 임의 확정 거부, 잘못된 정밀 시각의 카드 근거 미검증 표시, 일반 단어 오탐 방지, 빨간 검수 강조 확인. 집중 70개 통과(0.461초)는 별도 중간 결과.
- 저장: 수정/병합/채택/초안 반환이 단일 연결·동일 트랜잭션을 사용함을 검증. 반환 스냅샷 조회/디코딩 실패와 실제 SQLite deferred 제약을 이용한 COMMIT 실패에서 이전 값/버전·초안·확인 서명 rollback 확인. 포인터/전송 정책 실패 후 재생성 복원, 업그레이드 마지막 DDL 실패의 rollback·백업·재시도, 미래 스키마 거부 시 바이트 불변 확인. 집중 49개 통과(0.904초)는 별도 중간 결과.
- 최종 `.venv\Scripts\python.exe tools/verify_release.py`: `passed: true`, `live: false`. `compileall -q services ui tests tools`, `pip check`, `git diff --check` 통과. 기존 Pillow 폐기 예정 경고·의도적 재검토 실패 로그는 테스트 실패가 아님.

### 이전 단계별 결과

- V2 기준점 커밋 전 `.venv\Scripts\python.exe -m unittest discover -s tests -q` 재실행: **261개 통과, 25.883초**, 건너뜀 없음. 합성/mock, 실제 API 호출 없음.
- Git fetch 후 기준 HEAD와 `origin/main` 차이 0/0. 변경 대상의 키/토큰/개인키 패턴 검사에서 일치 없음. LF/CRLF 안내와 기존 Pillow 경고는 기능 실패가 아님.
- 전송 범위 보완: `.venv\Scripts\python.exe -m unittest tests.test_transfer_policy tests.test_v2_transfer_integration tests.test_v2_app_flow -q` **55개 통과, 3.988초**. 글자/낱말 재조합 거부, 가림 편집 재선택, 유사 식별자 오탐, 중간 수동 편집, 추가 가림 보호값 유지, mock 분석 요청에서 제외/유지값 확인. 실제 API 없음.
- 후속 날짜·카드·실제 창 보완 최종 전체: `.venv\Scripts\python.exe -m unittest discover -s tests -q` **321개 통과, 31.967초, 건너뜀 없음**. 중간 317개 통과 뒤 감사에서 추가 결함을 발견해 4개 회귀를 추가한 최종 코드로 재실행했습니다.
- 신규 `.venv\Scripts\python.exe tools/verify_v2_release.py`: **161개 통과, 13.110초, failures/errors/skipped/network_attempts 모두 0**. 소켓 연결/DNS를 차단하고 통신 시도는 예외를 잡아도 실패 처리합니다. 별도 `test_v2_release_gate` 3개로 통신 시도·건너뜀의 실패 판정/복원 검증. 중간 157개 결과와 최종 결과를 구분합니다.
- 날짜/출처: 기간 끝·오후 시간/24시간 표기 비교·미해석 표현 보존·SourceRef 좌표 유지·반복 날짜 셀 구분·잘못된 위치 경고. 누락 일정 참고 구역, 수정/삭제 날짜 미복원, 가린 원문 버전 사용, 참고 일정의 체크리스트/공개 안내 제외.
- 저장/복구: 확인 체크 철회, 확인 상태만 변경할 때 이전 초안 표시, 지연 응답/기존 초안 편집본의 이전 서명 보존, 렌더링 중 동시 수정의 낡은 본문 표시, 충돌 재비교·선택 적용·재충돌 보호, 삭제 전 미저장 카드 차단, DB 백업·가산 업그레이드와 구버전 초안 보존.
- 실제 앱 Tk geometry 검사: 컴팩트 **680×720** ↔ 일반 **1180×760**(일반 모드 기존 최소 높이) 전환. 입력 12개·하단 버튼 7개, 스크롤 처음/끝 근거 접근, 저장·미저장 종료 차단→취소→종료 확인. 독립 패널 시험에서 놓친 실제 앱 버튼 가림을 재현 후 수정했습니다. 네이티브 마우스 손 조작·화면 육안 검수나 배율 전수 시험은 아닙니다.
- 기존 `.venv\Scripts\python.exe tools/verify_release.py` 최종 `passed: true`, `live: false`. `pip check`, `compileall -q services ui tests tools`, `git diff --check` 통과. 기존 Pillow 경고·의도적 재검토 실패 로그는 유지됩니다.
- 후속 결함 점검과 개선의 시험 결과는 아래 이력을 덮지 않고 이 절에 추가합니다.

## V2 1차 — 2026-09-17 (기준점 `7b1be36` 이전 검증 기록)

합성 문자열·이미지·임시 SQLite 저장소와 OpenAI mock만 사용했습니다. 실제 공문·사용자 DB·실제 키를 검증 입력으로 사용하거나 유료 API를 호출하지 않았습니다. 기존 157개 시험 기록은 아래에 보존하며 이번 결과와 구분합니다.

### 실행 결과

- 전체 명령: `.venv\Scripts\python.exe -m unittest discover -s tests -q`. **최종 261개 통과, 50.145초, 건너뛴 시험 없음.** 직전 260개 전체 시험(50.132초) 후 원문 추출본/검수본 구분 보완과 관련 1개 시험을 추가한 최종 코드로 다시 실행했습니다.
- `.venv\Scripts\python.exe -m unittest tests.test_v2_app_flow -q`: 최신 앱 흐름 12개 통과(3.378초). 카드 수정·날짜 삭제·저장·앱 재생성·현재 할 일·새 초안, 이전 초안, 지연 응답, 정책 복원/범위 변경/충돌, 실제 UI→가린 픽셀 경계, 파일→텍스트 사본, 로컬 원문 요약 포함.
- `.venv\Scripts\python.exe tools/verify_release.py`: `passed: true`, `live: false`. 기존 캡처 저장·전사/분석 완료 표시·Tk 화면·업무 저장·완료·재열기·낡은 원문 거부·공개 초안 필터 통과.
- `.venv\Scripts\python.exe -m compileall -q services ui tests`: 구문 컴파일 통과. `.venv\Scripts\python.exe -m pip check`: 의존성 오류 없음. `git diff --check`: 공백 오류 없음.
- Windows Tk 초기화·창 구성·위젯 조작·재생성은 허용 실행 환경에서 수행했습니다. 기본 샌드박스의 일부 Tk 접근 시험은 `init.tcl` 접근 오류가 있어 해당 환경에서 다시 실행했습니다. 이번 결과는 실행 파일 패키징 성공을 의미하지 않습니다(저장소에 EXE 빌드 설정 없음).
- 초기 전체 시험은 238개 중 3개 실패: 기존 강제 OCR 검수 팝업 계약, 새 `on_document` 콜백 인자, 체크리스트 제목 호환. 새 계약/코드 보완 후 재실행했습니다. V2 앱 통합 시험은 과거 날짜가 자동 업무 제목에 남는 문제와 연도 미기재 날짜가 가려지는 문제를 잡아 수정했습니다. 이전 성공 수로 실패를 덮지 않았습니다.
- 의도적으로 주입한 복구 실패의 예외 종류 로그와 기존 Pillow `getdata()` 폐기 예정 경고는 남습니다. 초기 Tk 종료 후 예약 콜백 경고는 정리했으며 최종 전체 시험에서 재현되지 않았습니다.

### 인수 항목별 근거

다음의 ‘통과’는 합성/mock 및 로컬 저장·UI 계약 범위입니다. 실제 모델의 의미 판단 정확도는 별도입니다.

| ID | 결과 | 주요 시험/범위 |
| --- | --- | --- |
| A01 | 통과 | `test_v2_app_flow`, `test_work_card_store`, `test_card_outputs`: 수정→저장→재생성→동일 현재 날짜의 할 일/안내문 |
| A02 | 통과 | `test_v2_semantics`: 제출/행사/보고 역할, 없는 시간 생성 금지 |
| A03 | 통과 | `test_v2_semantics`, `test_card_outputs`: 연도·상대 기한·요일 충돌·여러 행사 열, 원문 표기/경고 유지 |
| A04 | 통과 | `test_v2_semantics`: 희망 학교, 제출 생략, ‘없음’ 회신 구분; 실제 AI 분류는 미검증 |
| A05 | 통과 | 일부/붙임 미확인 범위 안내와 근거 부족 분류, 전체 업무 없음 단정 방지 |
| A06 | 통과 | 가짜 인용/페이지 미인정, 중복 발췌 위치, Tk 근거 창/원문 버전 |
| A07 | 통과 | 명시적 빈값 삭제의 재분석·재실행 보존, 제목/AI 행동의 과거 기한 재등장 방지 |
| A08 | 통과 | 카드/원문/결과 편집 중 늦은 응답 미적용, 이전 입력 버전 이력과 정책 CAS |
| A09 | 통과 | 수정 전후 초안 별도 보존, 수동 편집 이력, 새 카드 채택도 이전 정보로 표시 |
| A10 | 통과 | 동일 카드 중복 담기 방지, 완료 상태 유지, 복사는 완료 아님 |
| P01 | 통과 | 일반 업무 연락처의 반복 팝업 없음, 첫 전송 선택은 명시적 |
| P02 | 통과 | 텍스트 패턴/문맥 후보, 이미지/파일 자동 검사 불가 안내; 전수 탐지 보증 아님 |
| P03 | 통과 | 실제 OCR mock request data URL 디코딩 후 검은 불투명 픽셀·가림 밖 픽셀 비교, UI 호출 인자도 확인 |
| P04 | 통과 | 분석 stream·재검토·후속 양식·도식화 mock payload에서 합성 비밀 제외, immutable file bytes/파일→텍스트에 원본 첨부 없음, 정책 재열기 |
| P05 | 통과 | 취소/마스킹 오류 요청 없음, 원본 fallback 없음, 전송 확대 확인, 최신 캡처 가림 병합·늦은 OCR 정책 덮기 거부 |
| R01 | 통과 | 기존 불완전 응답/타임아웃/거절·취소·중복 시작 보호 및 제한된 재시도/캐시 시험 |
| R02 | 통과 | 저장 실패 편집값 유지, SQLite 백업·기존 내 할 일 보존, 앱 재생성·출처 버전 복원 |
| R03 | 통과 | 기존 입력/캡처/표/저장/창/복사 회귀, 카드 기반 새 초안·원문 요약의 불필요 API 없음 |

### 한계와 1차 체크리스트

- [x] Tkinter/SQLite·기존 기능과 데이터를 유지하는 추가 저장 구조
- [x] 카드 편집·근거 확인·날짜 역할/조건/자료 부족/충돌 구분
- [x] 교사 수정·빈값·메모·완료 보존 및 연결 할 일/새 초안의 현재값 사용
- [x] 이전 초안 보존·버전 표시, 지연 응답/저장 충돌 보호
- [x] 4개 보호 모드 없이 전송 선택·실제 가림·후속 정책 적용
- [x] 자동 검사 한계 표시 및 실제 mock payload 검증
- [x] 2차 관련 공문 연결·새 알림·계정·클라우드 기능 미추가
- [ ] 실제 nano의 공문 해석·OCR·표/조건 연결 품질과 지연 시간 측정: 유료 API 금지 범위에 따라 미실행
- [ ] 사용자 손 조작에 의한 다중 모니터/배율·캡처 드래그, 모든 PDF/Office·EXE 배포 검증: 후속 필요

학부모·교직원 새 안내문은 사실값을 고정한 로컬 양식입니다. 실제 현장 문체·구성 만족도 및 모델 출력 품질을 mock 통과로 보증하지 않습니다. PDF/Office 가림은 파일 내부 재작성이나 숨은 레이어 제거가 아니라, 필요한 텍스트/페이지 이미지 사본으로 전환하는 기능입니다.

공식 기술 확인: [Structured Outputs](https://developers.openai.com/api/docs/guides/structured-outputs)는 스키마 준수와 의미 정확성을 별개로 다룹니다. [OpenAI 데이터 제어](https://developers.openai.com/api/docs/guides/your-data)에 맞춰 Responses의 `store=False`를 명시하되 모든 서버 보관이 없다고 안내하지 않습니다. 이 자료는 학교의 이용 허가를 의미하지 않습니다.

## 문맥·근거 검토 — 2026-09-17 (V2 이전 기록)

- `python -m unittest discover -s tests -q`: 최종 157개 통과(32.118초). 기존 136개에 분산 근거, 단일 경고, 선택적 복구, 실패/취소/캐시, 원문 셀 좌표, 안내 대상/제출처 구분 등 21개 사례 추가. 최초 테스트 중 표시 규칙 관련 2개 실패를 수정한 뒤 전체 재실행 통과. 재검토 실패 로그는 의도적인 오류 주입 시험 결과이며 테스트 실패가 아님.
- `python tools/verify_release.py`: 오프라인 임시 저장소에서 Tk 결과 표시·업무 저장·할 일 완료·재열기·낡은 원문 연결 차단 통과.
- `pip check`: 의존성 오류 없음. `git diff --check`: 공백 오류 없음. 기존 Pillow 폐기 예정 경고는 남음.
- 실제 API 시험에는 합성 텍스트만 사용. 사용자 공문·업무 DB·실물 캡처는 이번 시험에서 전송하지 않음.
- 초기 API 시험은 자동 테스트가 통과해도 기한 인용 재작성과 관성적인 확인 필요 분류로 실패했음. 원문 줄·셀 좌표 계약, 필드별 근거, 분류 프롬프트 및 추론 low 설정으로 보완. 실패 사실을 숨기거나 이전 통과 수로 실제 품질을 대체하지 않음.
- 합성 사례 1(흩어진 신청 기한/제출처, 다른 행사 기한, 교육지원청 명단 안내): 학교 체크리스트 1건, 올바른 기한·제출처, 불필요 경고 0건 확인. 마지막 통과 23.8초(재검토 포함). 제출 여부 플래그 추가 직전 측정이며 전체 최신 코드로 이 사례를 다시 유료 실행하지 않음.
- 합성 사례 2(담임의 수칙 안내, 제출 서류/기한 없음): 처음에는 학생을 제출처로 표시해 실패. `requires_submission` 분류와 비제출 업무의 제출 필드 제거 후 개별 재시험에서 체크리스트 1건, 잘못된 제출처/기한 없음, 불필요 경고 0건. 13.18초, 추가 재검토 없음.
- 별도 잘못된 기한 강제 복구: 최초 시험 1회 실패 후 개별 재시험 통과. 최초 실패 원인은 확정하지 않았으며 이후 실패 시 예외 종류만 기록하도록 추가(원문/키는 로그에 남기지 않음). 자동 시험으로 실패 시 결과 보존·캐시 금지·취소 전파를 검증.
- 위는 단계별 개별 통과 기록이며 마지막 전체 유료 스크립트가 한 번에 모두 통과했다는 뜻은 아님. 단일 표본의 시간·정확도를 일반화하지 않음.
- 모델은 기존 nano 유지. 첫 분석과 재검토는 유료이며, 재검토는 최대 8개/1회/30초/재시도 없음. 재검토 실패 결과는 캐시하지 않음. 외부 기관/참고 일정 및 검수 대기 업무는 정상 할 일 목록과 분리.
- 실제 원문 위치를 선택하더라도 다른 행사의 값을 잘못 연결하는 의미 오류는 여전히 가능. 이번 합성 사례가 모든 실제 공문 정확도를 보장하지 않으며 사용자 실패 입력의 현장 재검수는 남음.
- [공식 구조화 응답 문서](https://developers.openai.com/api/docs/guides/structured-outputs)를 확인해 API 스키마의 모든 필드 필수/추가 속성 금지를 적용. 로컬 과거 데이터 호환 기본값과 API 필수 계약은 분리.

재실행: `python tools/verify_context_review.py --live` (합성 API 3~5회). 개별 시험은 `--case 1`, `--case 2`, `--case repair`. 유료 호출에 동의한 경우에만 실행.

## 이전 검증 요약 — 2026-09-16

- 날짜·표 개선 및 표 보기 수정 후 자동 회귀 테스트 **136개 통과**. 마지막 전체 실행은 26.237초였으며 성능 보장 수치가 아닙니다.
- `pip check`: 의존성 오류 없음. `git diff --check`: 공백 오류 없음(LF/CRLF 안내는 있음).
- 제공 HWPX는 로컬에서만 읽어 표·일정을 검사했습니다. 실제 API에는 별도의 개인정보 없는 합성 표를 사용했습니다.
- 사용자 업무 데이터는 수정하지 않았습니다. 문서 정비 작업에서는 앱 테스트/API를 재실행하지 않았으며 아래 기능 결과는 직전 작업의 기록입니다.
- 알려진 경고: Pillow 테스트의 `getdata()` 폐기 예정 경고. 모든 실제 문서·PC에서의 무결함 판정이 아닙니다.

## 보완한 결함

1. 원문 인용과 별도로 담당·기한·제출물·제출처가 해당 인용에 포함되는지 확인합니다. 확인할 수 없는 값은 비우고 `확인 필요`로 표시합니다. 날짜는 연·월·일·시간을 비교하므로 같은 날짜의 다른 표기를 허용하고 원문 시간·예정·까지를 복원합니다. 연도 추측이나 여러 날짜 중 임의 선택은 하지 않습니다. 문자열 포함 검사는 의미적 정확성 보장이 아니므로 요약·행동 문구를 포함한 원문 대조는 필요합니다.
2. 학부모 안내의 내부 전달·제출 문장 필터를 확대하고 제목·본문·확인 질문의 휴대전화·이메일·주민번호 형태를 가립니다. 학교 공개 연락처라도 이메일/휴대전화 형태라면 가려질 수 있습니다. 이름·주소 등 모든 개인정보와 모든 표현의 내부 업무를 탐지하는 기능은 아니며 발송 전 공개 범위 검수가 필요합니다. 이 처리는 생성 결과에 적용되며 OpenAI로 보낼 입력을 비식별화하는 기능은 아닙니다.
3. 분석 결과와 원문의 지문을 별도 로컬 테이블에 저장합니다. 원문 또는 결과가 바뀌면 `내 할 일로 담기`를 차단하며, 앱 재시작 후에도 적용합니다. 이전 버전 결과는 연결 정보가 없으므로 재분석해야 합니다. 수동으로 결과를 수정한 경우에도 재분석이 필요합니다. 이미 담은 할 일은 원문 변경으로 자동 갱신하지 않습니다.

구형 분석 캐시는 새 검증 버전에서 재사용하지 않습니다. 기존 업무와 내 할 일 데이터는 삭제하지 않습니다.

## 이전 통합 검증 — 125개 테스트 시점

- 자동 회귀 테스트 125개 통과: 캡처 이미지 보관, OCR 요청·불완전 응답 보호, 분석 스트리밍·취소, 업무 저장·복원·동시 수정 보호, 캡처 검수, 설정, 내 할 일 선택·완료·중복 방지, 새 결함 재현 테스트.
- `pip check`: 의존성 오류 없음.
- `git diff --check`: 공백 오류 없음. Git의 LF/CRLF 안내는 남아 있습니다.
- `python tools/verify_release.py --live`: 통합 시나리오 통과. 합성 이미지 저장 → 실제 nano OCR → 실제 업무 분석·학부모 안내 생성 → Tk 결과 표시 → 업무 저장 → 내 할 일 완료 → 앱 객체 재생성 후 저장 데이터 복원 → 수정 원문 연결 거부.
- 해당 합성 문서에서 OCR 4.41초, 업무 분석 4.42초. 일반 문서의 성능 보장 수치가 아닙니다.
- Pillow 테스트의 `getdata()` 폐기 예정 경고는 남아 있으나 실행 실패는 없습니다.

## 재실행

저장소 루트에서 `.venv\Scripts\python.exe -m unittest discover -s tests -q`를 실행합니다.
`.venv\Scripts\python.exe tools/verify_release.py`는 네트워크 없는 통합 테스트입니다.
`--live`를 붙이면 합성 문서로 유료 API를 3~4회 호출합니다(재검토 여부에 따라 다름). 실제 사용자 문서는 보내지 않습니다.

날짜·표 회귀 및 환경 검사:

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -q
.\.venv\Scripts\python.exe -m pip check
git diff --check
.\.venv\Scripts\python.exe tools/verify_date_tables.py "테스트.hwpx"
```

마지막 명령은 로컬 검사만 합니다. `--live`를 추가하면 별도의 합성 표로 OCR·분석 API를 2~3회 호출합니다(재검토 여부에 따라 다름). HWPX 자체는 전송하지 않습니다. 실제 기밀 문서를 저장소에 복사하거나 시험 출력 전문을 커밋하지 마세요.

## 수동 확인이 남은 범위

- 실제 모니터에서 드래그 캡처, 화면 배율·다중 모니터, 축소 창의 시각적 배치.
- 모든 PDF/Office/HWPX 파일의 실물 호환성과 긴 표·작은 글씨·실제 공문의 인식 품질. 구형 HWP 안내 흐름도 실물 확인 필요.
- 이번 변경 이후 도식화 이미지의 실제 재생성은 하지 않았습니다. 관련 자동 테스트는 통과했으며 앞선 버전에서 합성 도식 생성은 확인했습니다.

## 날짜·표 후속 검증 — 2026-09-16의 136개 테스트 시점

- 자동 회귀 테스트 136개 통과(날짜 표기 정규화, 잘못된 날짜·시간, 연도 추측 방지, 다른 표 열 기한 혼동 방지, 병합 셀·본문 중복 제거 포함).
- 사용자 제공 충주컴퓨터꿈나무축제 HWPX를 로컬에서 읽어 다열 표 12개와 일정 항목 13개 확인. 배치용 단일열 표는 문단으로 처리합니다. 병합 셀은 `↳`로 출처를 표시해 반복하고 중첩 표는 셀 안 텍스트로 펼칩니다. HWPX 지면을 그대로 재현하는 기능은 아닙니다.
- 원문 일정에는 행사 열 제목·업무 행 이름·시간·예정을 유지하며 AI가 행동 항목을 놓쳐도 일정 메모에 표시합니다. 일정 항목 수는 중복 없는 행사 수가 아닙니다.
- 유효한 날짜는 청록색, 불확실 표기와 존재하지 않는 날짜는 빨간색. 청록색은 OCR 정확도 보증이 아닙니다.
- `tools/verify_date_tables.py <hwpx 경로> --live`: 실물 문서는 로컬만 사용. 합성 표를 실제 nano OCR에 전송해 4개 일정의 행사별 연결·기한·예정 보존, 실제 분석 결과의 일정 표시 검증 통과. OCR 5.28초, 분석 5.98초(한 번의 합성 테스트이며 성능 보장 수치 아님).
- 표 보기는 내용에 맞춰 열 너비를 조절하고 행 선택 시 셀 전체 내용을 표시합니다.

판정: 재현 결함 보완 및 자동·합성 API 검증 완료. 모든 환경·실제 문서를 포함한 무결함 판정은 아닙니다. 앱 재실행 후 기존 파일을 다시 읽거나 캡처를 다시 OCR 처리해야 새 추출 구조가 적용됩니다. 실제 모니터 드래그 캡처와 복잡한 표의 현장 검수는 필요합니다.

## 과거 비교 참고 — 현재 기본값과 구분

다음은 기존 README에 있던 초기 실험 기록을 옮긴 것입니다. 이번 문서 정비에서 재측정하지 않았으며 현재 모델 선택을 변경할 근거로 일반화하지 않습니다.

- 초기 112개 자동 테스트 통과 기록. Windows OCR·모델 선택 UI를 시험하던 시점이며 현재 UI 구성과 다릅니다.
- 합성 한글 공문 1장: Windows OCR 0.072초/공백 제외 문자 오류율 4.44%, nano 3.423초/0%, 4.1 mini 1.987초/0%. 표·작은 글씨·실제 공문 품질을 대표하지 않습니다.
- 초기 Luna 분석: 첫 미리보기 3.3초, 완료 4.23초. 현재 기본 분석 모델은 nano입니다.
- Flare 도식화 합성 실행안 1건: 실제 PNG 생성 19.21초 기록. 날짜·표 수정 이후의 실생성 재검증은 하지 않았습니다.
- Windows.Media.Ocr는 당시 개발 노트북에서 합성 이미지 읽기를 확인했으나 모든 PC의 지원을 검증하지 않았습니다. Windows AI TextRecognizer는 연동하지 않았으며 현재 기본 캡처는 nano입니다.

## 문서 정비 확인 — 2026-09-16

- README의 구형 역할·모델 메뉴, 예전 양식, 전체 날짜 빨간 강조, 환경변수 설명을 현재 코드와 대조해 정리했습니다.
- STATUS에 작업 이력·미완료 항목을 분리하고 AGENTS에 작업별 갱신 규칙을 추가했습니다.
- 네 문서의 로컬 링크 대상과 `git diff --check`를 검사했습니다. 실행 코드 변경이 없어 앱 회귀 테스트·유료 API는 추가 실행하지 않았습니다.
