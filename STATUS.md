# Ssokly 작업 상태와 기록

최종 갱신: 2026-09-17 · 현재 상태: 캡처 중심 재구성 3단계 구현·오프라인 검증 완료. 전체 551개 통과, 새 통신 차단 166개 통과. 1단계 `4b0ad92`, 2단계 `43900a8` 원격 반영. 3단계도 별도 커밋·푸시 단위이며 식별자/원격 반영은 Git 이력과 최종 안내로 확인한다.

[사용법](README.md) · [검증 결과](VALIDATION.md) · [기록 갱신 지침](AGENTS.md)

## 현재 기준

- 핵심: 캡처 → 자동 보관/선택적 OCR → 원본과 페이지 텍스트 대조·수정 → 복사/AI 정리. `main.py`는 독립 `CaptureDeskApp`을 연다.
- 화면: 왼쪽 보관함 / 가운데 확대·이동 가능한 원본 / 오른쪽 단일 편집창. 여러 캡처 페이지·순서·검색·썸네일, 창 안의 표 보기, 좁은 창 전환.
- 저장: 페이지별 수정본을 기준으로 전체 복사·검색·AI 입력을 구성. 700ms 자동 저장, 최초 OCR 별도 보존, 빈 수정본·재OCR·지연 응답 보호, 충돌 시 입력 보존/명시적 최신값 재열기.
- AI: 기존 키와 `gpt-5-nano` 유지. 요약 / 일정·할 일 텍스트 / 안내문(교직원·학부모·가정통신문). 카드 추출·추가 분석·재검토를 자동 실행하지 않는다.
- 전송: 최초 자동 OCR 동의/비용 확인, 보관만/가리고 읽기, 파일과 AI 사본 별도 선택. 기존 가림 상속·복원·범위 확대 확인·요청 직전 정책 확인. 로컬 열람·검색·수정·복사에는 AI 호출 없음.
- 기존 자료: Task/Capture ID·연결을 유지. 기존 업무·카드·할 일·초안은 읽기 전용 열람/복사, 통합 원문은 임의 분해하지 않음. 새 작업에서 카드 저장 경로 미호출. 기존 데이터 초기화·영구 삭제 없음.
- 복구: 가산 sidecar 업그레이드와 구형 캡처 DB 업그레이드 전 백업. PNG 보관 뒤 메타데이터 실패 자료는 현재 캡처 폴더의 검증된 정상 파일만 복구.
- 검증: 최종 전체 551개 통과(45.157초), 새 통신 차단 166개 통과(14.619초), 기존 V2 228개 통과(각 통신 시도 0). 기본 실행과 같은 DPI 설정의 새 화면/가림 인수 24개 통과(6.172초). 실제 사용자 DB·공문·유료 API는 사용하지 않음.

## 결정 사항

- 과거 카드 중심 구성은 사용자 승인 재구성(/10~12)으로 대체한다. 아래 과거 기록은 당시 결정/검증 이력이며 현재 사용법이 아니다.
- 모델·역할·필드별 검수 절차를 기본 작업의 조건으로 강제하지 않는다. 확인할 부분만 빨간 밑줄로 표시하고 원문은 직접 대조한다.
- 원문 없는 날짜·의무·담당을 추측하지 않도록 AI에 지시한다. 모델의 실제 사실/문맥 정확도는 별도 평가가 필요하다.
- 학부모 안내는 요청할 때 AI로 작성하고 결과 필터를 적용한다. 내부 보고와 분리하며 자동 발송하지 않는다. 과거 카드 기반 로컬 양식은 새 기본 흐름이 아니다.
- 입력을 자동 비식별화한다고 약속하지 않는다. 수동 가림·범위 확인과 기관의 외부 AI 기준을 따른다.
- Windows 로컬 OCR은 비교용 코드만 유지하며 기본 경로가 아니다. Windows AI TextRecognizer·구형 HWP 직접 읽기는 미구현.
- README는 현재 사용법, STATUS는 상태/누적 이력, VALIDATION은 실제 시험/한계. 기존 미커밋 문서 삭제는 이번 커밋들에서 제외한다.

## 남은 작업과 다음 우선순위

1. 별도 승인 후 현장 품질: 비식별 캡처/HWPX로 실제 nano OCR, 표의 행·열·날짜와 안내문 정확도를 원본 대조. 합성/mock 통과는 모델 품질 보증이 아니다.
2. 실제 사용 환경: 마우스 캡처·붙여넣기, 다중 모니터/고배율, 다른 PC 설치와 다양한 PDF/Office 파일.
3. 배포/이전: 실행 파일 패키징, 사용자 자료 전체 백업·PC 이전 절차. 이번에는 EXE 배포·데이터 이전 미수행.
4. 보류: 업무 카드/할 일 관리 재확장, 관련 공문 연결·신규 알림·대시보드·계정·클라우드 동기화·영구 삭제.

후속 후보 목록은 외부 전송·유료 시험·새 기능·배포를 자동 승인하지 않는다.

## 작업 이력

기록 초기화 이전 작업은 대화·코드·기존 검증 문서를 근거로 소급 정리했습니다. 당시의 정확한 작업 시각은 기록하지 않았으므로 같은 날 단계 번호만 사용합니다. 각 단계의 시험 수는 그 시점의 결과이지 현재 코드의 별도 재시험 결과가 아닙니다.

### 2026-09-16 / 01 · 노트북 개발 환경과 초기 개선 (소급)

- 요청: 다른 PC의 작업을 이어서 개발하고 OCR·분석 속도와 활용 양식을 개선.
- 기록: 현재 HEAD는 `34d332f`. 초기에는 Windows OCR·모델 선택 UI·구조화 분석·캐시·스트리밍·도식화 경로를 도입했으나 기본 모델과 선택 UI는 후속 단계에서 교체.
- 관련: `services/local_ocr.py`, `ai_service.py`, `analysis_cache.py`, `diagram_service.py`, `ui/app.py`, `tools/benchmark_ocr.py`.
- 검증: 당시 문서의 자동 테스트 112개 통과 기록. 합성 OCR 및 도식 PNG 생성 비교 기록은 VALIDATION의 과거 참고에 보존.
- 남음: 새 Windows AI TextRecognizer 연동은 완료되지 않았음. 당시 기본값을 현재 사용법으로 안내하지 않음.

### 2026-09-16 / 02 · 학교 업무 UI와 개인 할 일 (소급)

- 요청: 모델·담당 역할 선택 부담을 없애고 학교에서 쓸 수 있는 안내 양식 제공.
- 변경: OCR/분석 nano 자동 지정, 역할 선택 제거, 학교용 4종 양식과 별도 학부모 생성 구조, 선택한 체크리스트를 내 할 일로 저장·완료·재열기.
- 관련: `ui/app.py`, `services/analysis_document.py`, `ai_service.py`, `personal_todos.py`, `workflow_service.py`.
- 검증: 후속 125개 및 최종 136개 회귀 묶음에 관련 시험 포함. 이 단계만의 별도 통과 수는 확정하지 않음.
- 남음: 입력 개인정보는 자동 비식별화하지 않으며 결과는 발송 전 검수 필요.

### 2026-09-16 / 03 · 근거 검증·안내 필터·저장 연결 보호 (소급)

- 요청: 전체 점검과 보완, 최종 사용 검증.
- 변경: 원문 인용과 필드 근거 확인, 학부모 초안 내부 업무/일부 개인정보 필터, 원문·결과 지문을 저장해 낡은 결과의 내 할 일 추가 차단.
- 관련: `services/analysis_document.py`, `personal_todos.py`, `analysis_cache.py`, `tools/verify_release.py`, 관련 테스트.
- 검증: 당시 125개 자동 테스트와 합성 이미지→실제 OCR/분석→Tk 표시→저장/완료→앱 재생성 복원 통과. `pip check`, `git diff --check` 통과.
- 남음: 필터는 모든 개인정보·의미 오류를 보장하지 않음. 실제 모니터 드래그와 모든 파일 형식 호환은 미확인.

### 2026-09-16 / 04 · 원문 강조·표 보기·요약 (소급)

- 요청: 확인할 내용을 빨간색으로 표시하고 표 보기·요약 제공.
- 변경: 비파괴 텍스트 강조, 탭 기반 여러 표 보기/복사, 원문 요약 모드와 내부 분석 캐시 재사용.
- 관련: `services/source_review.py`, `analysis_document.py`, `ui/app.py`, `tests/test_source_review.py`.
- 검증: 당시 자동 테스트 129개 통과 기록.
- 후속 정정: 초기에는 정상 날짜까지 빨갛게 강조했으나 05 단계에서 청록색/불확실 빨간색으로 구분.

### 2026-09-16 / 05 · 실제 공문의 날짜·표 누락 개선 (소급)

- 문제: 기한이 있는데 일정이 사라지고 날짜 전체가 빨갛게 표시됨. HWPX 표가 문장으로 펼쳐지며 중복됨.
- 원인: 다른 날짜 표기를 문자 그대로 비교해 기한 제거, HWPX 중첩 문단 반복 추출, 표 셀 경계 손실.
- 변경: 날짜 의미 비교와 원문 시간/예정 유지, 표 열·행 문맥을 붙인 원문 일정, HWPX 셀 주소·병합 처리와 중복 제거, 캡처 표 프롬프트 보완, 색상 구분, 표 열 너비·행 상세 표시, 구형 분석 캐시 재사용 방지.
- 관련: `services/date_evidence.py`, `document_service.py`, `source_review.py`, `ocr_service.py`, `analysis_document.py`, `ai_service.py`, `analysis_cache.py`, `ui/app.py`, `tests/test_date_evidence.py`, `tools/verify_date_tables.py`.
- 검증: 자동 테스트 136개 통과. 제공 HWPX 로컬 추출에서 다열 표 12개·일정 항목 13개. 별도 합성 표의 실제 nano OCR→분석에서 4개 일정 연결 확인. OCR 5.28초·분석 5.98초는 단일 표본. 공문 원본은 해당 시험에서 외부 전송하지 않음.
- 남음: 기존 입력은 재읽기·재OCR 후 재분석 필요. 중첩 표 지면 재현, 모든 날짜 표현·실제 화면의 인식 정확도는 보장하지 않음.

### 2026-09-16 / 06 · 문서 정비와 지속 기록 규칙

- 요청: 뒤처진 README와 작업 문서를 갱신하고 STATUS에 작업을 꾸준히 기록.
- 변경: README를 현재 동작 기준으로 재작성해 구형 모델·양식·강조 규칙 및 완료된 개발 계획의 중복/충돌 제거. STATUS 신규 작성, VALIDATION의 최신/과거 시험 구분, AGENTS에 작업별 기록 갱신 규칙 추가.
- 관련: `README.md`, `STATUS.md`, `VALIDATION.md`, `AGENTS.md`.
- 검증: 실제 코드의 모델·캐시·환경변수·저장 경로와 대조. 문서 링크 및 `git diff --check` 확인. 문서만 변경하므로 앱 테스트와 유료 API는 재실행하지 않음.
- 다음: 실제 사용 피드백을 바탕으로 07번 이후 기록 추가. 이 규칙은 개발 작업 시 적용하며 백그라운드 기록이나 예약 자동화를 생성한 것은 아님.

### 2026-09-16 / 07 · 누적 작업 커밋·푸시

- 요청: 현재까지의 앱 개선·테스트·문서 작업 전체를 커밋하고 GitHub에 푸시.
- 변경: 코드·회귀 테스트·검증 도구·README/STATUS/VALIDATION/AGENTS를 함께 버전 관리. 새 기능 수정은 하지 않음.
- 대상: 기존 저장소 `hjpapa/Ssokly`, 로컬 `master`에서 기존 추적 브랜치 `origin/main`으로 일반 푸시. 강제 푸시는 사용하지 않음.
- 검증: fetch 후 기준 HEAD와 원격 main 차이 0/0 확인. 커밋 대상 파일·민감정보 패턴·공백 확인. 기능 검증은 직전 136개 통과 기록을 유지하며 이번 Git 작업에서는 앱/API를 재실행하지 않음.
- 제외: 실제 `.env`, 가상환경, `.local-results`, 사용자 공문·캡처·업무 DB. API 키와 사용자 데이터는 로컬 유지.
- 다음: 푸시 후 원격 커밋 일치와 작업 트리 상태 확인. 다른 PC는 해당 main을 pull한 뒤 기존 환경/데이터를 유지하고 앱 재시작.

### 2026-09-17 / 01 · 반복 경고와 공문 문맥 검토 개선

- 요청/문제: 한 업무에 담당·기한·제출물·제출처 경고가 연쇄 발생하고 안내/발표가 교사의 제출 업무로 변형됨.
- 상태: 구현 및 자동 검증 완료. 사용자 실제 입력에 대한 현장 검수는 남음. 이번 변경은 커밋·푸시하지 않음.
- 변경: 업무별 경고 묶기, 값 미기재/해당 없음과 오류 구분, 학교/외부 기관/참고 일정 분리, 검수 대기 항목의 체크리스트 제외. 필드별 별도 근거와 원문 줄·셀 좌표를 도입해 모델의 인용 재작성 방지.
- 재검토: 실패 항목 최대 8개를 추가 1회(30초, 재시도 없음) 요청. 오류/불완전 응답은 1차 결과 보존, 취소는 전파, 실패 결과는 캐시하지 않음. 새 검증 버전으로 이전 캐시 재사용 방지.
- 모델: 기존 nano 유지, 내부 분석 추론 minimal→low. 추가 호출 비용 안내를 화면에 표시. 최초 분석 지연이 늘 수 있음을 README에 명시.
- 관련: `services/analysis_document.py`, `source_contract.py`, `analysis_review.py`, `ai_service.py`, `analysis_cache.py`, `ui/app.py`, 관련 테스트·검증 도구 및 문서.
- 검증: 157개 회귀 테스트·오프라인 저장/재열기 통과. 실제 합성 API 시험 중 초기 인용 재작성/관성적 확인 필요/안내 대상을 제출처로 오인하는 실패를 발견해 좌표 계약·프롬프트·제출 여부 분류를 보완. 두 합성 공문은 개별 시험에서 불필요 경고 0건, 별도 강제 기한 복구도 개별 재시험 통과. 실측과 한계는 VALIDATION에 별도 기록.
- 남음: 원문 좌표의 존재가 업무 간 의미 연결까지 보장하지 않음. 실제 사용자가 문제를 겪은 입력을 재분석해 필수 업무 누락·잘못 추가한 업무·기한 연결·경고 수를 확인해야 함. 새 변경의 커밋/푸시는 별도 요청 시 수행.

### 2026-09-17 / 02 · 문맥 검토 개선 커밋·푸시

- 요청: 01 작업의 코드·테스트·문서를 커밋하고 푸시.
- 대상: 기준 커밋 `e326039` 이후 변경, 기존 `hjpapa/Ssokly`의 `origin/main`. 로컬 `master`에서 강제 옵션 없는 일반 푸시를 사용.
- 관련: 문맥 분석·원문 좌표·재검토 서비스, UI 비용 안내, 회귀 테스트·합성 검증 도구, README/STATUS/VALIDATION.
- 검증: fetch 후 로컬 HEAD와 원격 main 차이 0/0. 커밋 대상 및 민감정보 패턴·공백 검사. 기능 시험은 직전 157개 통과 기록을 유지하며 이번 Git 작업에서 앱·유료 API는 재실행하지 않음.
- 제외: 실제 `.env`, 가상환경, 로컬 시험 산출물 및 사용자 문서·업무 DB.
- 다음: 푸시 뒤 원격 커밋 일치·작업 트리 확인. 실제 사용 원문 재분석과 현장 검수는 여전히 필요하며 실행 파일 배포와는 별개.

### 2026-09-17 / 03 · V2 1차 업무 카드와 전송 사본 보호

- 요청/문제: `SSOKLY_IMPROVEMENT_V2.md` 기준으로 공문→카드→근거→교사 수정→저장→새 안내문의 연결을 개선.
- 상태: 코드 구현과 합성/mock 자동 검증 완료. 실제 API·실물 문서 품질/성능·배포 검증은 수행하지 않음. 요청 파일은 수정하지 않았고 커밋·푸시·배포도 하지 않음.
- 변경: 기존 Tkinter/SQLite/nano 유지. 대상/담당, 필수/조건부/안내/추가 제안/판단 유보, 제출/행사/보고/개인 준비일 분리. 연도·상대 기한 표기 유지, 날짜/요일 충돌 구분. OCR/파일 추출본과 교사 검수본 버전 분리.
- 보존: 확실한 원문 대응만 재분석 병합, 불명확 대응은 비교 후보. 교사 수정·명시적 빈값·메모·완료 유지. 내 할 일 기존 DB 백업 후 참조 테이블 추가. 카드 수정은 연결 할 일과 새 초안에 반영하며 기존 초안은 버전과 함께 보존.
- 전송: 선택 전 API 없음. 실제 텍스트/불투명 RGB 사본만 전송, PDF/Office 가림은 텍스트/페이지 이미지 사본으로 전환. 정책과 이미지 좌표 복원, 새 캡처 가림 제한 병합, CAS로 늦은 OCR의 정책 덮어쓰기 차단. 취소/가림 실패 시 원본 fallback 없음. Responses 요청 `store=False` 명시(모든 서버 보관 부재라는 의미 아님).
- 관련: `ui/app.py`, 신규 `ui/work_cards.py`, `ui/transfer_dialog.py`; `services/work_card_store.py`, `workspace_state.py`, `card_outputs.py`, `transfer_policy.py`; 분석/근거/날짜/전사/할 일/업무 저장 서비스, 관련 회귀 시험, README·STATUS·VALIDATION.
- 검증: `python -m unittest discover -s tests -q` 전체 재시험, `python tools/verify_release.py` 오프라인 Tk 저장·재열기, `python -m compileall -q services ui tests`, `python -m pip check`, `git diff --check`. 실제 결과 수와 소요 시간은 VALIDATION 최신 절 참고.
- 실패와 보완: 이전 승인 UI 계약 시험을 V2 선택 계약으로 갱신; 자동 제목/AI 행동 문구의 과거 날짜 재등장 차단; 연도 없는 유효 날짜가 통째로 확인 필요로 숨겨지던 출력 수정; 외부 기관 체크박스와 학부모 조건/회신정보 누락 수정; 종료 후 Tk 예약 콜백 정리.
- 남음: 승인된 비식별 평가셋으로 실제 nano 해석 품질을 후속 평가. 다중 모니터·배율별 손 조작, 모든 PDF/Office 형식, 실행 파일 패키징은 미검증. 2차 기능은 별도 요청 전 보류.

### 2026-09-17 / 04 · V2 기준점 검증·단계별 동기화 시작

- 요청: 특별한 이슈가 없으면 후속 구현을 계속하고 단계별로 커밋·푸시.
- 상태: V2 1차 변경의 전체 재시험 완료. 기존 `master`→`origin/main`으로 커밋·일반 푸시하며 강제 푸시는 사용하지 않음.
- 관련: 03 단계 코드·테스트·사용법과 V2 요청 문서. `.env`, 가상환경, 실제 공문·업무 DB·로컬 산출물은 제외.
- 검증: fetch 후 기존 HEAD/원격 main 차이 0/0, 민감정보 패턴 검사(파일명만 출력) 이상 없음. 전체 261개 테스트 통과(25.883초). 테스트는 합성/mock이며 유료 API 없음.
- 다음: 전송 정책·날짜/조건·카드 편집 복구 경로의 재현 결함을 점검·수정하고 다음 커밋으로 분리. 2차 기능 확장·유료 평가·EXE 배포는 승인 없이 진행하지 않음.

### 2026-09-17 / 05 · 가린 전송 범위의 재조합·오탐 보완

- 기준: V2 1차 `7b1be36`을 `origin/main`에 일반 푸시 완료. 후속 구현은 별도 커밋으로 분리.
- 문제: 승인 사본의 글자·낱말 재조합이 전송 검사에 통과하고, `A001` 가림 시 남겨야 할 `A002`까지 차단되는 비교 오류를 합성 입력으로 재현.
- 변경: 연속 발췌/공백 등가만 허용하고 바뀐 가림 원문은 사본 재선택. 식별자·연락처·가림 토큰 단위 비교, 직접 선택 가림과 수동 편집 분리 기록. 추가 가림에는 기존 제외값 유지.
- 관련: `services/transfer_policy.py`, `ui/transfer_dialog.py`, `ui/app.py`, 전송 정책/API 경계/앱 흐름 테스트, README·VALIDATION.
- 검증: 합성/mock 집중 55개 통과(3.988초). 실제 UI 승인 후 mock 요청의 제외값 부재·남긴 값 보존 확인. 실제 API·키·공문 미사용.
- 다음: 기간/시간 보존, 표 근거 위치, 저장 충돌 복구, 작은 창, 확인 상태와 초안 버전 연결을 별도 단계로 검증·커밋·푸시.

### 2026-09-17 / 06 · 날짜·카드 복구·실제 창 배치와 오프라인 릴리스 검증

- 기준: 전송 보호 보완 `7480f65` 원격 반영 완료. 특별한 이슈가 없으면 구현·단계별 커밋·푸시를 계속하라는 요청에 따른 후속 단계.
- 날짜: 축약 기간 끝·오전/오후 시간을 보존, 미해석 표현의 검수 사유 유지. 실제 선택한 줄/셀을 카드까지 전달·독립 재검증하고 캐시 버전 갱신. AI가 연결하지 못한 일정은 원문 참고 구역에 표시하되 검증된 위치로 교사가 수정·삭제한 날짜 재등장 방지. 가린 사본 분석은 해당 안전 원문 버전만 사용.
- 카드: 현재값 비교·필드 선택 재적용·편집 취소로 충돌 복구, 확인 체크 철회·확인 상태 별도 서명, 내 할 일과 새 초안에 동일 현재값 표시. 기존 DB 백업 후 가산 마이그레이션, 구버전 초안 본문은 유지하고 확인 기록 미상 표시.
- 안내: 학생부·학생지원센터·학부모회 등 조직을 학생/학부모 개인 행동으로 오인해 내부 기한을 공개 초안에 넣던 필터 보완.
- 감사와 보완: standalone UI에서는 통과했지만 실제 앱에서는 버튼 7개가 숨는 문제를 재현해 고정 행/스크롤로 수정. 미저장 카드가 있는 현재 업무 삭제의 고립 편집 잠금 차단. 렌더링 도중 다른 창에서 수정되어도 당시 카드 버전/확인 서명을 저장하여 낡은 본문을 최신으로 잘못 표시하지 않음. 확인한 AI 행동 문구의 옛 날짜도 별도 필드 수정 뒤 재등장하지 않음.
- 관련: 날짜/분석 계약·카드/할 일/양식 저장 서비스, `ui/app.py`, `ui/work_cards.py`, 신규 회귀 사례, `tools/verify_v2_release.py`, README·VALIDATION.
- 검증: 최종 전체 321개 통과(31.967초, 건너뜀 없음). 네트워크 차단 V2 161개 통과(13.110초, 통신 시도 0). 기존 `verify_release.py` 오프라인 통과, 컴파일·의존성·공백 검사 통과. Tk 실제 앱 680×720/1180×760 왕복·근거 접근·저장·미저장 종료 차단/취소 복구 확인. 모두 합성/mock·임시 DB, 유료 API 없음.
- 남음: 실제 모델 품질·사용자 손 조작/배율·실물 형식 호환·EXE는 미검증. 관련 공문 연결·새 알림·클라우드 등 2차 확장은 수행하지 않음. 원본 요청 문서·기존 키·실제 업무 DB는 변경하지 않음.

### 2026-09-17 / 07 · 컴퓨터 종료 후 재개·초안 버전 복구

- 요청: 컴퓨터 종료 뒤 기존 작업 계속. 재개 시 `a575f16`까지 보존됐고 fetch 후 원격 main 차이 0/0, 미커밋 변경 없음 확인.
- 문제: 초안 이력/포인터 저장과 업무 본문 자동 저장 사이에 종료되면, 재열기에서 예전 본문에 최신 초안 버전이 붙는 결함을 합성 Tk 시험으로 재현.
- 변경: 재열기 시 포인터뿐 아니라 실제 본문 일치를 검사. 유일한 이력만 복구하고 불일치/다중 후보는 버전 미확인 표시. 본문·모든 이력은 보존. 실제 전원 차단/디스크 손상을 재현한 시험은 아님.
- 관련: `ui/app.py`, `tests/test_v2_app_flow.py`, README·STATUS·VALIDATION.
- 검증: 재개 기준점 네트워크 차단 V2 161개 통과(14.230초). 새 재현 시험 1개 초기 실패 → 수정 후 앱 흐름 22개 통과(4.729초, 복구 회귀 3개 포함). 실제 API·실물 자료·사용자 DB 미사용.
- 다음: 날짜의 미해석 시간 접미부 유실과 DB 저장 성공 후 재조회 실패의 오인 문제를 별도 단계로 수정·검증·커밋·푸시. 새 2차 기능은 추가하지 않음.

### 2026-09-17 / 08 · 시간 표현 보존과 저장 성공 판정 보완

- 요청/문제: 재개 후 기존 1차 범위의 남은 안정성 점검. 원문의 `오후 2시 반`에서 ‘반’이 사라지거나 AM/PM이 누락되어 정밀 시각처럼 검증되는 결함, DB commit 뒤 반환값 재조회 실패가 이미 완료한 저장을 실패로 알리는 결함을 합성 입력으로 재현.
- 상태: 구현·회귀 검증 완료. 직전 재시작 복구 `6689517` 푸시 완료, 이번 개선은 별도 커밋·일반 푸시.
- 날짜 변경: 명시 AM/PM 앞·뒤 표기 처리. 반·초·대략 시각·이어지는 범위를 원문에 유지하고 부분 해석 검수 표시. 오전/오후가 한쪽만 나온 범위의 시각을 임의 확정하지 않음. 확정할 수 없는 AI 시각은 검증 거부하고 원문 참고 일정에 전체 표현 유지. 원문 빨간 강조 연동, 분석 캐시 계약 v10.
- 저장 변경: 카드 수정/분석 병합/비교 후보 채택/초안 저장의 반환 스냅샷을 같은 트랜잭션 안에서 읽고 검증. commit 이후 별도 조회 실패가 저장 실패로 뒤바뀌지 않도록 함. 실제 조회·디코딩·commit 실패는 rollback. 데이터 스키마·키·모델 변경 없음.
- 관련: `services/date_evidence.py`, `source_review.py`, `analysis_cache.py`, `work_card_store.py`; 날짜/카드 회귀, 신규 `tests/test_storage_faults.py`, `tools/verify_v2_release.py`, README·STATUS·VALIDATION.
- 검증: 신규 날짜 9개·저장 13개, 직전 복구 3개 포함 전체 346개 통과(32.262초). V2 차단 검증 186개 통과(13.560초, 통신 시도 0). 기존 오프라인 통합·컴파일·의존성·공백 검사 통과. snapshot 이후 실제 deferred 제약 commit 실패, 부분 쓰기 실패, 가림 정책/포인터 보존, 스키마 업그레이드 실패/재시도/백업 유지 검증.
- 남음: 모든 날짜 문법·실제 전원/디스크 장애·모델 품질·실물 문서/모니터/EXE는 보장하지 않음. 후속은 승인된 비식별 자료의 현장 평가이며 이번에는 실제 API·사용자 업무 DB·2차 기능을 사용/변경하지 않음.

### 2026-09-17 / 09 · 긴 표·병합 셀 일정 연결과 표 검수 개선

- 요청/문제: 다음 작업 계속. 기존 1차 범위에서 다단·반복 머리글의 기한/행사 오연결, 셀 안 제어문자 유실과 잘못된 HWPX 좌표, 표의 긴 내용·검수 표시를 보완.
- 상태: 구현·최종 검증 완료. 직전 `2bab5ae` 원격 반영 확인. 이번 개선을 단계별 커밋·일반 푸시 단위로 반영하며 식별자와 원격 반영 여부는 Git 이력으로 확인.
- 일정: 같은 열의 다단 머리글 조합, 반복 머리글의 행사 순서 재설정, 다른 행사 셀의 내용이 행 라벨에 섞이는 문제 보완. 모호한 제목·열 수 불일치·병렬 행사 식별 열은 행/열 확인으로 안내하며 확정 업무나 내 할 일을 자동 추가하지 않음.
- HWPX: 줄바꿈·탭 구분, 빈 셀·병합 셀 좌표 보존, 중첩 표의 설명 표시가 셀 본문에 섞이는 문제 수정. 잘못된 좌표/병합/겹침/섹션은 안전한 오류로 처리하고 임의 잘라내기·덮어쓰기 금지.
- 표 보기: 추출 원문 행 번호, 첫 행 상세 자동 표시, 빈 셀 명시, 선택 행/전체 표 원본 TSV 복사, 상세의 실제 검수 부분만 빨간 밑줄 표시. 명시된 단일행 HWPX 표도 인식. 원문과 업무 카드는 변경하지 않는 스냅샷 창. 원문 강조도 탭·개행을 넘나드는 숫자를 날짜로 잘못 합치지 않도록 보완.
- 관련: `services/date_evidence.py`, `document_service.py`, `source_review.py`, `ui/app.py`, 신규 `ui/source_tables.py`; 신규 표/HWPX/UI 회귀 및 앱 연결 시험, `tools/verify_v2_release.py`, README·STATUS·VALIDATION.
- 검증: 중간 집중 60개/전체 381개 통과 뒤 감사에서 추가 3종 결함을 찾아 회귀 4개 보강. 최종 `unittest discover -s tests -q` **385개 통과(54.678초)**, `tools/verify_v2_release.py` **228개 통과(25.268초, 통신 시도/실패/건너뜀 0)**. 기존 오프라인 통합·컴파일·의존성·문서 링크·공백 검사 통과. 모든 입력은 합성/mock·임시 DB이며 유료 API·사용자 공문·업무 DB 미사용.
- 남음: 실물 HWPX·캡처 OCR 모델 품질과 원본 지면 재현·중첩 표 독립 뷰·배율/EXE는 미검증. 2차 신규 기능은 추가하지 않음. 다음은 비식별 현장 사례에서 원문/카드/참고 일정 대조이며 실제 문서 전송은 별도 승인 범위에서만 수행.

### 2026-09-17 / 10 · 캡처 중심 재구성 1단계: 저장·화면 기반

- 요청: 캡처 보관→페이지별 텍스트 대조/편집/복사→선택적 AI 정리 중심으로 재구성. 업무 카드·할 일 관리·도식화를 새 기본 흐름에서 제외하되 기존 자료는 보존.
- 상태: 기반 구현·집중 검증 완료. 이 단계에서 기본 진입점은 바꾸지 않으며 새 주 화면·전송 연결은 다음 단계에서 검증한다.
- 변경: 기존 Task/Capture ID를 사용하는 별도 문서 저장 계층. 페이지 순서·현재값과 원래 OCR·빈 수정본·결과 이력·휴지통·설정·충돌 검사, 가산 업그레이드 전 백업. 기존 기록은 읽기 전용이며 원본 중복 복사·영구 삭제 없음. 여러 DB를 완전한 단일 트랜잭션으로 만들지는 않으며 부분 저장 실패 자료도 기존 기록/개별 캡처로 발견 가능하게 유지.
- 화면 기반: 원본 1장과 보이는 영역만 확대 렌더링, 이동/맞춤/100%, 창 안의 표와 셀별 검수 표시, 최대 128개 썸네일 캐시.
- 관련: `services/document_library.py`, `ui/desk_widgets.py`, 각 신규 시험 및 README·STATUS·VALIDATION.
- 검증: `.venv\Scripts\python.exe -m unittest tests.test_document_library tests.test_desk_widgets -q` 최종 38개 통과(2.147초). 저장 계층 신규 28개, 위젯 신규 10개. 저장 관련 기존 회귀 포함 63개 통과(2.170초)는 별도 집중 실행. 합성 파일/임시 DB/mock만 사용.
- 남음: 새 전체 화면 저장/이동 보호·자동 OCR 동의와 가림·카드 없는 AI 정리·기존 기록 열람·전체 검증·기본 실행 전환. 사용자가 삭제한 기존 개선안 문서는 복원하거나 커밋에 포함하지 않음. 실제 API·사용자 자료 미사용.

### 2026-09-17 / 11 · 캡처 중심 재구성 2단계: 독립 화면·OCR·AI 연결

- 요청: 3열 캡처 보관·페이지별 편집·복사 흐름, 명시적 자동 OCR 동의와 기존 가림 보호, 카드 없는 간단한 AI 정리 구현.
- 상태: 구현·집중 검증 완료. 1단계 `4b0ad92` 원격 반영 확인. 새 화면은 모듈로 실행 가능하며 `main.py` 기본 진입점은 3단계 최종 검증에서 전환한다.
- 변경: 제목/본문 검색·썸네일·페이지 추가/순서·확대/이동·단일 텍스트와 표 탭·700ms 자동 저장·선택/페이지/전체 복사·좁은 창 전환. 빈 수정본·페이지 이동 전 저장 실패 입력·재OCR 뒤 수정본 유지. 휴지통 원본 보존·복원과 기존 기록 읽기 전용/통합 원문 사본 가져오기.
- 처리: 최초 자동 인식의 전송/비용 동의, 보관만/가리고 읽기, 이미지 먼저 저장, 파일 전송 별도 확인, 전송 정책 상속/CAS/요청 직전 확인. OCR·AI는 기존 nano와 키를 재사용하며 업무 카드 생성/분석 재검토는 호출하지 않음. 요약·일정/할 일 텍스트·대상별 안내문은 문서 전체 수정본 또는 선택 부분으로 단일 요청. 결과는 원문과 별도 저장, 늦은 응답/직접 편집 보호와 이전 결과 열기.
- 관련: `ui/capture_desk.py`, `services/text_actions.py`, `desk_transfer.py`, `legacy_reader.py`, 신규 UI/전송/결과/기존 기록 회귀, README·STATUS·VALIDATION.
- 검증: 신규 화면/흐름/기존 기록/AI/전송/복구 최종 집중 84개 통과(8.596초). 중간 75개 통과(7.062초), 별도 복구는 6개에서 제목 동시 수정·다른 페이지 OCR 최신값 로드 3개를 추가해 9개 통과(1.579초). 초기 취소 대기 작업·스레드 시작 실패의 처리 잠금, 좁은 창 하단 버튼 가림, 이전 결과의 낡은 스냅샷·휴지통 복원 문제를 보완. 합성/mock·임시 DB·Windows Tk만 사용, 실제 API·사용자 자료 미사용.
- 남음: 최종 전체 회귀·통신 차단 gate·기본 진입점 전환·README 현재 사용법 전면 정리. 실제 OCR/AI 품질·다중 모니터/배율·실행 파일 패키징은 별도 검증이며 이번 승인으로 실물 공문을 전송하지 않음. 기존 미커밋 문서 삭제는 계속 제외.

### 2026-09-17 / 12 · 캡처 중심 재구성 3단계: 기본 전환·복구·최종 검증

- 요청: 새 화면을 기본 진입점으로 전환하고 기존 자료/전송 정책의 비파괴 호환, 캡처 저장 실패·재시작·작은 창 등 최종 인수 기준을 검증. README를 현재 구성으로 정리하고 단계별 커밋·푸시.
- 상태: 구현·합성/mock 최종 검증 완료. 앞 단계 `4b0ad92`/`43900a8` 일반 푸시 성공 확인. 이번도 별도 커밋·일반 푸시 단위이며 최종 식별자는 Git 이력에서 확인. 미커밋 `SSOKLY_IMPROVEMENT_V2.md` 삭제는 포함하지 않음.
- 기본 화면: `main.py`가 독립 `CaptureDeskApp`을 실행. DPI 설정은 import 부작용 없이 실제 main 실행 때 적용. README는 캡처·편집·복사·간단 AI 중심 현재 사용법만 설명하며 과거 카드/도식화 설명을 기본 사용법에서 제거.
- 캡처 복구: PNG 파일 저장 뒤 메타데이터/실제 COMMIT 실패에도 원본 유지. 시작/검색 시 현재 보관 폴더의 정상 앱 형식 PNG만 복구, 임의 이름·손상·심볼릭/하드 링크·변경 중 파일은 제외. 기존 OCR·수정본·휴지통 상태를 덮지 않음. v1 캡처 DB는 변경 전 SQLite 백업·명시적 트랜잭션으로 업그레이드 실패 원복. 이미지 자체 저장 실패나 모든 실제 디스크 장애를 보장하는 것은 아님.
- 원문/가림: 최초 OCR을 sidecar 설정에 한 번만 보존(빈값/재시작/실패 원복 포함)하고 원래 인식 보기에서 최근 인식본과 구분. 기존 자료는 남아 있는 원문부터 보존. 이미지+문서 가림이 함께 있으면 동일 픽셀의 기존 좌표만 복원하고 별도 통합 전송 범위 확인은 유지. 손상 PNG 표시 오류가 보관함/이미지 뷰 전체를 중단하지 않게 보완.
- 관련: `main.py`, `services/capture_store.py`, `document_library.py`, `desk_transfer.py`, `ui/capture_desk.py`, `desk_widgets.py`, `transfer_dialog.py`, 새/보강 회귀와 `tools/verify_capture_desk.py`, README·STATUS·VALIDATION.
- 검증: 최종 전체 551개 통과(45.157초), 새 통신 차단 gate 166개 통과(14.619초, 실패/오류/건너뜀/통신 시도 0), 기존 V2 228개 통과(통신 시도 0), 기존 오프라인 통합 `passed: true, live: false`. 실제 시작과 같은 DPI의 새 화면/가림 인수 24개 통과(6.172초). 컴파일·의존성·문서 링크·키 패턴·공백 검사 통과.
- 시험 과정: 첫 전체 535개 중 기존 화면 크기 시험 1개 실패. 실행 모듈 import 시 전역 DPI 변경을 main 안으로 옮겨 관련 15개 통과 후 위 최종 전체 재실행. 3장 캡처→mock OCR→수정/빈값→재정렬→앱 재생성→검색/정확 복사/전체·선택 AI 입력, 350행 표/3200×2400 이미지/720×560 최소 창, 늦은 결과·실패·정책 변경을 검증.
- 남음: 실제 모델 품질·실물 공문·네이티브 캡처/클립보드 손 조작·다중 모니터/배율·모든 Office 형식·EXE 패키징은 미검증. 실제 사용자 자료/키를 시험 입력으로 사용하거나 유료 API를 호출하지 않았으며 기존 키와 사용자 삭제 상태는 유지.

## 다음 기록 양식

```text
### YYYY-MM-DD / NN · 작업명
- 요청/문제:
- 상태: 진행 중 / 완료 / 중단 (검증·배포 상태는 별도)
- 변경:
- 관련 파일:
- 검증: 명령·결과 또는 미실행 사유
- 남은 과제/다음 단계:
```
